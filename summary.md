# InterpretabilityTransfer 研究方向与进展

本文只保留理解研究问题、关键实验进度和结论所必需的信息；模型形状、单次运行数值和完整证据链等细节不在这里展开。冻结结论、限制、撤回和当前计划以 [`docs/INTERPRETABILITY_TRANSFER_AUDIT.md`](docs/INTERPRETABILITY_TRANSFER_AUDIT.md) 为准；尚未晋升的最新实验进度见 [`docs/EXPERIMENT_LOG.md`](docs/EXPERIMENT_LOG.md)，并在本文标为初步结果。

**更新日期：** 2026-08-11

## Research Objective

1. **从基础因素比较模型体系。** 分析纯文本、纯蛋白质和语言–蛋白质联合生成模型在分词、训练语料和模型架构上的差异。
2. **使用并审计可解释性方法。** 用可解释性方法研究模型行为、表征和因果计算，同时检查方法本身是否可信。
3. **提出并验证用于生物知识研究的方法。** 设计能够区分模型知识、语料检索和表面相关的可解释性方法；通过因果、检索和独立生物数据验证后，再用它提出和检验新的生物学假设。

表中的百分比表示关键验证环节的粗略完成度，不代表结论可信度、计算量或论文完成度。

## Objective 1：从基础因素比较模型体系

| 编号 | 假设 | 关键验证 | 实验进度 | 结论 |
|---|---|---|---|---|
| R1.1 分词与输出接口 | 分词、输入格式和输出空间能解释部分、但不是全部文本与蛋白质差异。 | 比较不同分词方式的纯模型，并检查联合模型两种模式的渲染、输出和可测量性。 | 60%。纯模型对照已完成；五个联合 checkpoint 完成资格检查，其中一条谱系两种模式都可测量。 | 小残基词表会限制输出空间，错误渲染也足以制造虚假差异；但分词不能解释所有方法迁移失败。联合模型的蛋白质模式多采用多残基分词，其数值不能与逐残基模型直接比较。 |
| R1.2 训练语料与知识来源 | 训练语料和适配阶段会改变蛋白质能力的来源，并可能牺牲原有文本能力。 | 将模型表现与训练语料同源检索比较，并沿匹配训练谱系观察两种模式何时出现或消失。 | 60%。纯蛋白质模型的检索界、联合模型匹配谱系和组件交换已完成；家族外和时间切分验证尚待进行。 | 当前适应度接口没有证明三个纯蛋白质模型超越训练语料检索。联合模型的主要文本代价出现在蛋白质继续预训练阶段；最新组件交换初步显示，该代价主要随 Transformer 主体移动，而蛋白质能力需要主体与词表接口协同适配。 |
| R1.3 模型架构与计算路径 | 架构和模态可能改变 MLP、注意力和路由的分工。 | 比较形状匹配的文本/蛋白质模型、Dense/MoE 蛋白质模型，以及合格联合模型的两种模式。 | 60%。纯模型的主要架构与路由对照，以及联合模型的双模式扰动和组件交换均已完成；模型规模对照尚未运行。 | MoE 架构和路由不是模型替换失败的单独原因。最新结果初步显示，扰动鲁棒性取决于具体 checkpoint 和训练阶段，不能概括为蛋白质模态的固有属性。 |

## Objective 2：使用并审计可解释性方法

| 编号 | 假设 | 关键验证 | 实验进度 | 结论 |
|---|---|---|---|---|
| R2.1 局部表征与干预 | Lens、Probe、Erasure 和 Activation Patching 在接口匹配后可以回答可比的局部问题。 | 分开检查信息能否被解码、行为是否依赖它，以及干预是否损害无关行为。 | 35%。纯模型完成了部分实验；联合模型尚未进入方法实验。 | 这些方法能提供局部证据，但稳定解码不等于因果依赖，也不能单独构成完整机制解释。 |
| R2.2 注意力与 PAA | 注意力模式或 PAA 选择器能够找回真正影响输出的注意力头。 | 用穷举路径修补建立因果标签，并与随机机会和只使用层位置的简单选择器比较。 | 60%。纯文本和纯蛋白质模型的主要因果审计已完成；字母表碰撞对照尚待补做。 | PAA 在文本模型上有效，但在已测逐残基蛋白质模型上不可靠；蛋白质模型仍存在有因果作用的归纳注意力头。架构匹配对的结论成立，更广的面板级模态结论仍待碰撞对照。 |
| R2.3 稀疏字典、Transcoder 与模型替换 | 如果稀疏字典保留了模型计算，逐层替换后应保持原模型行为。 | 在可比预算下替换 MLP，并用 Dense/MoE、分词、线性基线和不同序列区间作控制。 | 75%。五个纯模型的主要行为对照、稳健性检查和神经元基线已完成；联合模型已有合格 checkpoint，但尚未进入替换实验。 | 文本模型的替换忠实度显著高于蛋白质模型，且差距不是归一化造成的。已有单因素解释均不足；最新控制初步将问题缩小到字典学习本身，但不同蛋白质模型可能以不同方式失败，近端原因仍未确定。 |
| R2.4 同输入 Model Diffing 与 Crosscoder | Model Diffing 能够区分训练阶段引入、删除或保留的稳定特征，Crosscoder 能在简单对齐不足时表示这些差异。 | 在匹配 checkpoint 上使用完全相同的输入，先比较原始差异、线性对齐和独立字典，必要时再训练 Crosscoder，并用干预验证模型特有特征。 | 0%。匹配谱系和行为锚已经建立，Crosscoder 实验尚未开始。 | 尚无训练阶段特有特征或方法忠实度结论；基础模型的蛋白质模式不可测，只能作为适配前的表征参照。 |
| R2.5 测量方法审计 | 先确认任务可测，再检查行为和因果忠实度，可以区分真实失败与评价问题。 | 依次检查接口、可测量性、统计功效、分母、行为保持、因果目标和独立数据稳定性。 | 75%。流程已用于纯模型实验和五个联合 checkpoint 的资格检查。 | 这套流程修正或撤回了多个表面结论，并在联合模型上先发现渲染和模态坍塌问题。重建误差、探针准确率、注意力模式或单一恢复率都不能独自证明机制；这是目前最成熟的方法学成果。 |

## Objective 3：提出并验证用于生物知识研究的方法

| 编号 | 假设 | 关键验证 | 实验进度 | 结论 |
|---|---|---|---|---|
| R3.1 概念对齐 Lens | 将不可比的 token 输出投影到预定义概念空间，可以恢复有意义的比较。 | 在蛋白质内部使用生物属性，跨模态只使用两侧都有定义的概念，并加入随机概念、因果干预和未见家族验证。 | 10%。失败位置已确定，方法尚未实现。 | 已具备开展有限验证实验的依据，但还没有方法结果。 |
| R3.2 语言中介的因果概念对齐 | 文本与蛋白质模式可能对同一生物概念形成可校准的共同表征或因果响应。 | 使用真实序列–描述配对，在序列或预定义概念层次测试检索、线性和正交对齐，必要时再训练小型 Adapter，并用概念干预验证。 | 0%。双模式 checkpoint 已完成资格检查，对齐实验尚未开始。 | 尚无共享概念或跨模态激活对齐结论；文本和蛋白质 token 不能直接配对。 |
| R3.3 跨模态因果电路翻译 | 对同一生物概念分别建立可信的蛋白质和语言 Circuit 后，两者的节点和因果边可能形成可验证的映射。 | 先让两侧 Circuit 分别通过节点和路径干预，再检查蛋白质 → 语言 → 蛋白质往返一致性，并用未见家族和独立生物证据验证映射后的预测。 | 0%。两侧 Circuit 忠实度和概念对齐等前置条件尚未满足。 | 尚无 Circuit 翻译结果。 |
| R3.4 生物知识验证与发现 | 通过审计的方法可以区分模型编码的生物知识与同源检索或表面相关，并据此产生可检验的新假设。 | 使用未见家族、同源检索、传统基线和外部表型或功能证据验证已知机制；只有验证通过后，才预注册并检验新的机制预测。 | 20%。纯蛋白质模型的检索界已完成；真实机制和新知识发现尚未验证。 | 当前适应度接口未显示三个纯蛋白质模型超越语料检索，但这不等于模型只是在记忆。尚无经过验证的新生物机制或新生物学知识。 |

总体上，Objective 1 和 Objective 2 已过半，Objective 3 仍处于方法设计前期。联合模型已经用于训练阶段和组件级干预，但其模型替换、Model Diffing、概念对齐和生物知识验证尚未开始。
