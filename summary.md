# InterpretabilityTransfer 研究方向与当前结论

更新日期：2026-09-25

文本和蛋白质生成模型都通过序列预测进行训练，但相似的训练目标是否对应相似的能力，以及这些能力依赖哪些计算过程，仍需在明确的任务和对照下检验。本项目比较纯文本、纯蛋白质和语言–蛋白质联合生成模型，依次回答三个问题：模型能完成哪些任务；现有可解释性方法能否解释这些表现如何形成、表示和计算；当现有方法不足时，如何发展新方法，区分模型行为中的序列统计规律与生物信息。

方向一的结论按科学结论组织，共六项。总的方向是：蛋白生成模型能够编码超出简单序列统计的生物学信息，但这种信息并不随生物学约束复杂度的提高而均匀延伸。已支持的是蛋白预训练在实验测量的突变效应上确立的一阶边界（并在一份独立数据、一个不同的被测量上重现）、局部与单残基层面的约束、折叠稳定性上的落地，以及原生条件输入改变生成物的类别归属；在合格测量下未检出的是四状态循环上的表征交互对比（33 个检查点无一在三个划分上高于零）；受测量所限而不是受模型限制的有四项：三阶终点、以接触为条件的结构富集、远端同源终点，以及我们自己预先声明的局部基序对照。冻结表征优于原生似然这一读出层结果不再作为独立结论，而是作为对原生似然读法的校准并入第一项，原因是复评显示它带来的是广度而不是幅度。区分这四类结论是本工作的主要贡献之一，逐项证据与待完成项见下节。

## 比较设计与测量原则

比较首先需要区分模型差异的来源。同谱系的多个检查点描述训练配置或规模变化伴随的表现差异；同一权重下的文本与蛋白模式控制部分架构因素；Llama-2 到 ProLLaMA 的连续阶段则用于追踪蛋白适配的收益与代价。公开检查点的语料、架构、规模和训练过程并未随机匹配，因此这些比较本身不能识别模态或参数量的因果效应。下表列出各组的比较作用，列入面板不表示每个检查点都完成了所有任务。

| 比较作用 | 检查点 | 必须保留的区别 |
| --- | --- | --- |
| 文本基础模型与规模对照 | GPT-2、GPT-2-medium、GPT-2-large、GPT-2-xl；Qwen2.5-0.5B/7B/32B；Llama-3.2-3B；Qwen3-8B-Base | 原生文本能力与氨基酸字符串计分是不同任务；BPE 字符串似然不是逐残基计分 |
| 文本语料与基本单位对照 | DialoGPT-small；ByGPT5 small/base/medium | DialoGPT 改变语料；ByGPT5 提供字节级、在本次氨基酸串上逐残基对齐的对照 |
| 蛋白多残基分词对照 | ProtGPT2 | 原生 FASTA 格式；多残基 BPE 不允许任意残基级干预对齐 |
| 蛋白原生类别条件 | ZymCTRL | 输入需 EC 标签；缺标签的 ProteinGym 和 MegaScale 任务不能强行计分 |
| 蛋白逐残基谱系 | ProGen2 small/base/medium/large/xlarge；RITA-xl | 方向与边界标记、输出词表和精度分别声明；同谱系也须在共同支持集上配对 |
| 蛋白稀疏 MoE 与稠密参照 | ProGen3 112M/3B；ProtGPT3-1.3B；ProteinGLM-7B-CLM | 不混用总参数与激活参数；ProteinGLM 为稠密 CLM，非 MLM 或旗舰模型；训练语料未核实部分保持未知 |
| 语言–蛋白联合谱系 | Galactica 125M/1.3B/6.7B/30B；InstructProtein | 原生蛋白块格式不同；InstructProtein 的文本输出空间不支持合格双模式比较 |
| 同谱系训练阶段 | Llama-2-7B→ProLLaMA Stage 1→Stage 2 | 共享架构和 tokenizer；继续预训练与指令微调的变化分别读取，蛋白任务收益与文本代价同时报告 |

输入格式和计分单位决定了可以提出什么问题。这里的原生接口是模型训练时使用的输入输出格式，包括序列边界、方向标记和必要的类别条件；token 是分词后的基本单位，可能对应一个或多个残基。实验固定这些格式、实际计分位置、完整输出词表和推理精度，载入失败或声明不符时停止测量。逐残基计分与多残基 BPE 字符串计分分别报告，不直接比较不同 tokenizer 的 nats/token。模型声明的上下文上限也不等于本项目实际采用的计分窗口。

在解释模型内部计算之前，还需确认测量能否识别上下文信息，即模型相对独立参考集拟合的一元频率基线，是否利用了超出单个符号频率的信息。默认测量窗口为 384 tokens；每个检查点和模式使用 8 个区块，每区块 200 条记录。分析按近重复组重采样，并校正参考集位移；校正区间下界大于零才识别为正信号，用作归一化比值的分母时还需满足精度要求。旧的固定幅度门槛已退出正式判决。Galactica-125M 蛋白模式、InstructProtein 文本模式和所测 DialoGPT-small 区块未通过这一识别，但这不排除它们在其他任务上具有可测表现；Galactica-125M 的 ProteinGym 结果仍保留。

跨模型差值须基于相同候选、评分方向和统计单位。蛋白数据按近重复组、家族或设计系列留出与重采样，并在全体合格记录中以固定随机种子置换抽样。精确去重不足以排除近亲，家族隔离也不等于完整预训练排除。序列、参考集、代码和产物版本均需固定，保留逐记录统计与失败原因；旧接口污染、错误计分跨度、退役门槛和 ESMFold v1 的结果不与修正版本合并。

本文按科学问题组织现有证据，区分已完成的阴性结果、接口不适用、统计未识别、已关闭和未运行的实验。详细发现、历史修正与当前计划以[科学审计](docs/INTERPRETABILITY_TRANSFER_AUDIT.md)为准，配置和产物见[实验日志](docs/EXPERIMENT_LOG.md)。

## 方向一：生物学能力图谱的六项结论

本节按科学结论组织，而不是按实验或执行顺序：每一项结论先陈述它声称什么，再给出支持它的证据，具体实验只作为证据出现。总的结论是，蛋白生成模型能够编码超出简单序列统计的生物学信息，但这种信息并不随生物学约束复杂度的提高而均匀延伸；本研究要指出的是能力在哪里泛化、在哪里减弱，以及表面上的边界有多少来自模型、有多少来自测量框架。最后一点是本工作与基准测试的区别所在，而且已有四项结果明确属于测量边界而不是模型边界：达不到自身通道间噪声下限的三阶终点、分辨不出接触与非接触的成对终点、两个蛋白酶通道系统性不一致的远端同源终点，以及我们自己预先声明却降低了所增强基线的局部基序对照。此外还有两项横跨面板的测量限制单独记录：批量为一时在结构上空洞的精度复核，以及无法跨留出簇泛化的 k-mer 对照。

序列评分、条件生成和上下文利用检验不同的能力。已有序列的实验标签用于评价模型的排序表现；生成物的家族归属和结构预测则提供间接证据，不能沿用已有蛋白的实验标签，因此各项终点的结论分别解释。

四类结论必须分开表述，本节在每项结论下使用同一套词汇：

| 结论类型 | 含义 | 不能读成 |
| --- | --- | --- |
| 已支持的能力 | 在资格化的终点、资格化的对照和声明的读出类别下，模型侧的量被判为高于零 | 不等于机制，也不等于该能力延伸到未测的约束或家族 |
| 在合格测量下未检出 | 终点与对照都已资格化，模型侧的量被判为不高于零 | 不等于信息不存在，只说明在该读出类别与标签预算下取不到 |
| 未判定 | 仪器、支持集或读出无法在现有材料上作出判断 | 不能与“未检出”合并，也不能当作阴性结果引用 |
| 受测量所限的结论 | 判断被测量框架而不是被模型限制，且已定位到具体环节 | 不是关于模型的结论；只有修正测量才能推进 |

此外，三个层次在全节中始终分开，不能互相代入：冻结似然赋予什么，这是原生行为；一个拟合的读出能从冻结状态里解出什么，这是可读出的表示，该层结论以所用读出类别为条件；以及干预会显示模型实际使用了什么，这是因果使用，本文不测量这一层，只有方向二的试点触及它。把三者合并会让每一条结论都被读得过强。

### 结论一：蛋白预训练确立了一条可重现的一阶能力边界

第一项结论是面板上最干净的一项：在实验测量的突变效应上，以蛋白序列预训练的检查点越过了指定的序列基线，而以文本语料预训练的检查点没有；这条边界在一份两个开发面板都没有用到的数据、一个不同的被测量上重现。同一节用读出复评来校准这条边界的读法：更丰富的表征读出会让更多检查点出现可识别的信号，却几乎不改变信号的大小，所以论文的承重读数是原生似然。

在同一个 201 个实验、163 个家族、25,728 个变体（每个实验 128 个标签盲抽取的变体）的共同支持集上，原生似然对突变效应的排序从 ProGen2-xlarge 的 +0.39206 [+0.36438, +0.41839] 一直排到 ByGPT5-medium-en 的 −0.02027 [−0.04147, +0.00086]，14 个字面氨基酸串文本检查点中有 13 个的原始区间覆盖零。这是面板上最大也最干净的能力差异，而它是似然赋值上的差异，不是读出能力上的差异。该共同支持集与各检查点每个实验最多 1,000 个变体的原生支持集不是同一个集合，两者的数值只分别报告，不相减。把突变排序、设计蛋白稳定性、同源上下文利用与生成四个方向并置来看，能力不落在单一坐标上：同一个检查点可以在一个终点上超过指定基线，而在另一个终点上落在更简单的参照之下，这一点由后面几项结论分别给出证据，彼此不互相代入。本节引用的区间与限制以[审计记录](docs/INTERPRETABILITY_TRANSFER_AUDIT.md)的主张登记表与限制目录为准。

#### 突变排序与序列基线

ProteinGym 通过实验测量突变效应，可用于检验模型赋予序列的似然是否与实验排序一致。基础集包含 217 个替换突变实验、174 个家族单位；受上下文长度限制，部分检查点的分析集为 201 个实验／163 个家族，另一些为 213／171。原生支持集分别报告，需要配对时使用实际共同支持集。

仅有正相关不足以说明复杂模型带来了额外预测信息。我们同时比较逐位 profile 基线与 BLOSUM62：前者用参考序列构建各位置的残基分布，后者用氨基酸替换分数评价突变。表中的 MODEL−LOOKUP 表示模型与逐位 profile 的 Spearman 相关系数之差；相关系数及差值均无量纲，区间为按家族重采样得到的 95% 置信区间。

| 证据 | 当前结果 | 能支持的解释 |
| --- | --- | --- |
| ProGen3 的具名检查点差异 | 修正标记计分后，在 217 个实验／174 家族上，112M 的 MODEL−LOOKUP 为 −0.0804 [−0.1132, −0.0465]，3B 为 +0.0568 [+0.0360, +0.0786]；同集原始 Spearman 增量 +0.1373 [+0.1097, +0.1660] | 八月旧产物包含非残基标记，现已被修正计分取代；检查点差异不能识别参数量效应 |
| 稠密蛋白参照 | ProteinGLM-7B-CLM 在 201 个实验／163 家族上，MODEL−LOOKUP 为 +0.0384 [+0.0175, +0.0592]；RITA-xl 在同等大小支持集上为 +0.0075 [−0.0141, +0.0282] | ProteinGLM 超过该外部 profile；RITA 的区间尚不能区分正负差异，不等于与基线等效 |
| ProGen2 与其他蛋白对照 | ProGen2 五档没有建立超过各自 profile 的显著优势；ProtGPT2、ProtGPT3-1.3B 也没有建立该优势。多个检查点超过 BLOSUM62 | 替换表与逐位 profile 是不同强度的参照；超过前者不能代替超过后者 |
| 联合模型的评分边界 | Galactica 四档的原始 Spearman 随具名检查点增加，但各档仍低于 LOOKUP 和 BLOSUM62。30B 在 213 个实验／171 家族上，MODEL−LOOKUP 为 −0.2076 [−0.2393, −0.1782]。InstructProtein 及 ProLLaMA 两阶段也未超过 profile | 正相关和训练阶段收益均可存在于基线以下；联合模型的原生接口不保证这个任务上的优势 |
| 纯文本氨基酸串对照 | 十三档均低于 LOOKUP 和 BLOSUM62；Qwen2.5-32B 在 217 个实验／174 家族上的原始 Spearman 为 +0.0328 [+0.0139, +0.0510]，其余十二档的原始相关区间覆盖零 | ByGPT5 三档为残基对齐子集；其余为字符串 BPE 对照。文本预训练中的网页蛋白内容污染未定量，不能把此结果当成严格无蛋白暴露实验 |

这些结果支持 ProGen3-3B 和 ProteinGLM-7B-CLM 在各自分析集上超过已实现的 profile 参照。ProGen2 五档及多个其他模型尚未建立同样的优势，说明超过替换表与超过逐位 profile 是不同层次的结果。这里的 LOOKUP 对 ProGen3、ProteinGLM、Galactica 和 InstructProtein 等模型来自外部 UniRef50，ProGen2 的训练语料覆盖也不完整；它不是完整训练语料的检索上界，因此正差值仍不能排除更强的检索解释。

ProGen3 采用修正后的 R242 计分，排除了三个非残基标记；代码修复和冻结清单已解释其与八月旧产物的差异。逐实验分数和输入摘要已有本地记录，终态运行回执与实验日志仍需补齐。ZymCTRL 则因缺少必需的 EC 标签而跳过全部 217 个实验，这属于接口不适用。纯文本对照还存在未定量的网页蛋白内容暴露，不能视为严格无蛋白训练的对照；其支持集与计分方法见[氨基酸串计分协议](docs/PROTEINGYM_TEXT_AA_FP32_V1.md)。

#### 独立数据上的重现

同一条排序在两个开发面板都没有用到的数据上、以一个不同的被测量重现。在一份结构域丰度深突变数据的 96 个野生型家族组上，把模型似然加入一个已经移除无效应零模型约一半平方误差的资格化对照集之后，33 个检查点中有 2 个在平方误差单元上高于零，没有一个低于零；在同一次拟合的域内秩单元上有 14 个高于零，而这 14 个恰好就是以蛋白序列预训练的检查点，不论接口如何，11 个逐残基接口全部在其中，19 个文本检查点全部未被识别。酵母互补测得的丰度不是 ProteinGym 的被测量，所以这是跨被测量的佐证而不是重复测量；可识别的增量只相当于对照集剩余平方误差的百分之一到百分之二，与开发终点上一样是可识别而小。这份数据在生物学距离轴上的位置见结论五。

#### 读出复评：校准这条边界的读法，而不是另立一项结论

原生似然只是同一次前向计算的一个读出，并不是最好的读出，所以这条边界需要用更丰富的读出来校准。在 99 个检查点×划分单元中的 69 个上，冻结状态的拟合读出优于原生似然；但把 1,024 维投影表征加入似然、profile 与序列描述符构成的同预算监督基线后，33 个无条件检查点中只有 3 个的区间高于零，30 个没有可识别的额外信息。决定性的是面板自带的伪证对照：14 个把蛋白序列当作字面氨基酸串计分的文本检查点，在全部 42 个单元上“表征减似然”都高于零（+0.06975 至 +0.17027），而它们相对同预算基线的增量在全部 42 个单元上都低于零（−0.01115 至 −0.03573）。“表征所含信息超过模型自身分数”在文本模型上同样出现，因此这个比较为正不能许可任何关于蛋白特异表征内容的主张——它属于校准，不是一项独立结论。

读出类别与逐层深度的复评都已完成，结论是把数量而不是上限往上移。跨两条轴共有九个检查点在某一区块或某一类别上出现可识别的正增量，而在原先采用的读出下只有三个；锚定面板的上限只从 +0.022549 [+0.011958, +0.033015] 移到 +0.028818 [+0.018414, +0.039207]，且三个划分中有一个几乎没有变化。七个被抬起的单元里，五个选到了原先两个深度没有挂到的区块，另外两个选到的是原先确实读过的末层，所以广度的限制一部分来自跨深度的汇总方式，而不只是挂了哪些层。字面氨基酸串那一层在五个读出类别的 53 个单元和 1,638 个深度单元上都没有任何一个高于零，其中最大的点估计为 +0.000772 且本身未被识别，所以复评恢复出来的东西是蛋白特异的。这条校准要说的正是：更丰富的读出扩大了广度，没有实质提高幅度——广度的边界属于提取与汇总，幅度的边界属于表征本身。四个关卡在复评后的重算尚未返回，所以表征层面的判决一律暂定。

### 结论二：蛋白模型捕捉到局部序列与单残基层面的生物学约束

第二项结论说的是似然侧：在本项目测得最强的对照之上，原生似然仍然携带突变效应信息，而在三个划分上都携带它的 14 个检查点恰好就是以蛋白序列预训练的那些。表征侧在同一对照上的残留已随这一关卡的全面板重算确认，读数不变（99 个单元、11,880 个节点，1,098 个有跨零风险而无一跨越），单独记录，不参与这项结论。

把对照从组成换成逐位 profile 再换成资格化的局部窗口对照，模型侧增量被吸收的比例逐步上升，但在三个检查点上始终没有被吸收干净。这条主张的力量来自对照本身先通过了资格检验：一个降低自身基线留出相关系数的对照不能界定任何东西，而现在用的对照在三个划分上都提升它所增强的基线。

[交叉对照实验](docs/D1_HIERARCHY_CROSSED_CONTROLS.md)在同一队列上把参照分层，五个检查点×三个划分共 15 个拟合单元，零失败。相对只含组成、突变身份、位置和长度的 444 维参照，表征增量为 ProGen3-3B +0.10635 至 +0.11112、ProteinGLM +0.14306 至 +0.14665；加入 14 个突变局部 profile 坐标后分别降到 +0.03666 至 +0.04104 和 +0.04720 至 +0.05570，即 profile 吸收了约三分之二，剩余的 +0.037 至 +0.056 只在这两个检查点上于三个划分均高于零。Llama-2 文本父模型的增量相对组成参照在三个划分上都低于零（−0.03348 至 −0.01798），ProLLaMA 两阶段相对组成参照均未被识别。预先声明的局部模式参照（二肽计数差与三肽计数差的投影）在这个队列上并不成立：加入它反而把参照自身的留出相关系数降低 0.04747（相对组成参照）和 0.01684（相对组成加 profile 参照），三个划分同向。因此“越过局部基序这一层”没有得到预期意义上的判决——这是需要更强局部参照的方法缺口，而不是模型已经越过该层。分词接口描述符几乎不改变结果：参照自身变化不超过 0.00023，任一检查点的增量变化不超过 +0.00103，没有任何符号或区间判定改变。该对照向全面板的扩展已准备并通过接口资格检查，但尚未拟合，其中没有任何结果。

局部化学与局部上下文这道关卡已经给出预期意义上的判决：六个候选局部对照在任何拟合之前声明并绑定摘要，其中两个因降低所增强的基线而被弃用，被保留的 111 列窗口对照在三个划分上都提升两条基线且全部贡献区间高于零，由此得到本项目测得的最强对照集（+0.47906 至 +0.48290，对比 +0.44590 至 +0.45316）。结论是：通过资格检验的局部对照并不吸收模型侧的增量，但确实吸收其中可测的一部分——就 ProteinGLM-7B-CLM 而言，相当于其相对组成参照增量的 26%、以及在 profile 对照之后仍存留部分的 44%；在 C+P+wall 之上仍然留存、且三个划分区间全部高于零的只有三个检查点（ProteinGLM-7B-CLM +0.02605 至 +0.03033、ProGen3-3B +0.02174 至 +0.02487、ProGen2-base +0.01022 至 +0.01916）；Llama-2 父模型与 ByGPT5 在三个划分中的两个上低于零。这九个检查点的分析仍是预先声明的主分析，随后在全部 33 个检查点上重跑同一测量作为可推广性检验——而检验推翻了其中的一个概括：原先“留存的表征残留属于逐残基分词的蛋白接口”这一读法不成立，因为 RITA-XL、ProtGPT3-1.3B 与 InstructProtein 都是以蛋白语料训练、且每个残基恰好一个 token，却没有在三个划分上全部留存。全面板上三个划分全部高于零的是 8 个检查点（ProGen2 五档 +0.01006 至 +0.01667、ProGen3 两档 +0.01046 至 +0.02487、ProteinGLM-7B-CLM +0.02605 至 +0.03033），它们共有的东西比“接口类别”更窄——就是 ProGen2、ProGen3 与 ProteinGLM 这几个检查点本身——关卡对此不作任何解释，这是应有的克制。表征侧的读数以所用读出类别为条件，五个关卡的重算已确认判定不变。把概括提出来、再用更宽的面板检验、检验不过就撤回，这个顺序本身值得记录：这正是资格化纪律在起作用。该关卡的分词分析同时排除了“分词切分”这一解释：按实测切分分层，五个每残基一 token 的检查点中有三个留存、两个都不留存，而四个多残基检查点中只有一个在两个划分上留存。这九个检查点是覆盖各层的选样而不是全部名册，且包含读出实验中唯一三个可识别为正的检查点，因此这一排列是对已查看结果的延伸而非独立确认；扩展到全部 34 个检查点的测量正在进行，面板级结论尚未确定。此外还有一项独立结果：一个只看突变位点周围 7 个残基窗口的 280 列预测器，单独拟合即达到 444 列组成参照留出相关系数的约 90%，说明该参照本身的强度有相当部分来自局部信息。

似然侧的结论则相反地推广了，而且更锐利：在 C+P+wall——本项目测得的最强对照——之上，原生似然的增量在 33 个检查点中的 14 个上三个划分区间全部高于零，而这 14 个恰好就是以蛋白序列预训练的那些检查点（ProGen2 五档、ProGen3 两档、ProteinGLM-7B-CLM、ProtGPT2、ProtGPT3-1.3B、RITA-XL、InstructProtein 与 ProLLaMA 两阶段）。另有两个只在一个划分上可识别（Galactica-30B、Qwen2.5-32B），恰好一个有任一划分低于零（ByGPT5-base-en，一个划分），没有任何检查点在三个划分上都低于零；Galactica 四档与 Llama-2-7B 父模型这些“以文本语料训练却读蛋白”的检查点在任何划分上都没有可识别的增量，字面文本各档也没有。也就是说：一个通过资格检验的局部对照，加上组成描述符与突变局部 profile 统计，合起来无法解释以蛋白预训练的模型其原生似然中的突变效应信息，而对所有未经蛋白预训练的模型则完全可以解释。这条界线是由预训练语料划出的，不是由接口、分词或规模划出的；由于似然以单个标量列进入比较，它不带暂定标记，也同时是结论一的证据。分词切分的排除也随之更强：15 个多残基切分的检查点中没有任何一个的表征增量在三个划分上可识别，而 18 个“每残基一 token”的检查点中有 8 个可识别——但其中 10 个同样不可识别，所以“每残基一 token”在这个面板上是必要的，却远不充分。

两点限定必须与这项结论同时引用。被保留的窗口对照包含一个以 BLOSUM62 在窗口内评分的分块，而 BLOSUM62 由比对家族估计，因此这条“局部”对照并不脱离进化统计；它与 profile 对照是并列的强对照，而不是与进化统计对立的纯局部参照。另一方面，只看突变位点周围 7 个残基窗口、不含绝对位置、不含序列长度、窗口之外一无所见的 280 列预测器，单独在留出簇上拟合即达到 +0.27559 [+0.24053, +0.30991]／+0.27076 [+0.23579, +0.30355]／+0.27380 [+0.23866, +0.30722]，相当于 444 列组成参照（+0.30024／+0.29725／+0.30194）的 91.8%／91.1%／90.7%；111 列的窗口描述符并集为 85.3%／87.4%／84.4%。由此有两条结论都要记录：被称作“组成与长度”的对照并不因此就不含局部信息，因为这个队列上的突变效应排序有约九成（相对组成级描述符所能达到的水平）是突变近邻的函数；而该窗口预测器在 C+P 之上只增加 +0.00082 至 +0.00871，自身却已达到 +0.27，说明它所提供的大部分内容也被突变局部 profile 统计提供了——换言之，profile 对照的表面强度有很大一部分是通过比对列表达出来的局部信息，而不是另一类信息。两条结论都不意味着更长程的信息不存在；它们说的是在这一读出类别与标签预算下，超出七残基窗口之外可触及的部分很小。

本结论中依赖表征增量的部分是暂定的：按全程排序规则，表征层面的重算已在五个关卡上全部返回，没有任何一条判定改变；相对同一对照的似然增量不在此列，它是现成结论。

模型独立排序低于 profile，不代表其分数没有互补信息。控制指定的标量 profile 后，ProGen3-3B 与 ProteinGLM 在无上下文条件、ProLLaMA 两阶段在同源条件，均保留偏秩相关并改善家族留出预测。这些增量针对所实现的参照和预测器，不排除更丰富的进化统计解释。历史评分的同源深度、长度等分层进一步描述相对表现变化，但仍是观察关联。

### 结论三：建模能力在组合型残基依赖处减弱

同一批检查点在单点突变终点上可复现的增益，到了四状态循环这一组合型终点上基本消失：表征交互对比在 33 个检查点、两个支持集上都没有任何一个在三个划分上高于零，似然交互只有 3 个检查点在三个划分上高于零，而承载结论的是一阶对照——单突变似然差本身的增量比交互对比大 3 至 8 倍。因此这里的减弱不是“模型对相互作用一无所知”，而是可复现的模型侧信号停留在一阶量上。

双突变非加性实验检验更高一层的边界，先单独资格化标签工具（[标签工具记录](docs/D1_PAIRWISE_LABEL_INSTRUMENT.md)），全过程不读取任何模型量。终点是野生型中心的循环量 ε = y_AB − y_A − y_B + y_WT，单位 kcal/mol。预注册的收录规则只约束合并估计，会让一个蛋白酶通道未被识别：在它接受的 669,363 行里有 15,116 行的胰蛋白酶值正好落在 ±15.000 kcal/mol 的拟合边界上，该通道置信宽度的 99 分位达到 24.3 kcal/mol，按这一规则得到的 136,824 个天然循环两通道一致性只有 Pearson r = +0.165 [+0.131, +0.210]。因此在任何模型结果出现之前，规则被收紧为两个通道的置信宽度各自都在 [0, 0.5] kcal/mol，代价是排除 14.05% 的行和 27.3% 的天然循环，位点对数从 513 降到 499。收紧后的一致性为 r = +0.885 [+0.847, +0.915]，共享成分标准差 0.789 [0.684, 0.906] kcal/mol，通道间不一致标准差 0.284 [0.257, 0.312] kcal/mol，比值 2.78 [2.34, 3.26]；两个通道在绝对 ε 上并不互换，胰凝乳蛋白酶平均高出 +0.200 [+0.155, +0.246] kcal/mol。两个事实是约束而不是注脚：独立单位是位点对而不是循环——99,510 个循环只落在 499 个位点对上，其中 99,493 个（99.98%）位于源研究自己挑选的耦合表上，因此这里的非加性频率与幅度都不估计位置对总体的取值，序列间隔分层也不是接触分层；而非线性加性对照是必须的——ε 与加性预测的相关为 −0.704 和 −0.675，扣除按加性预测分成二十个分位的响应曲线可吸收 53.8% 与 51.5% 的 ε 方差，响应幅度分别为 2.53 与 2.44 kcal/mol，扣除之后跨通道仍可复现（r = +0.809 [+0.759, +0.848]）。

支持集与成对序列基线的准备见[成对基线准备](docs/D1_PAIRWISE_BASELINE_READINESS.md)：家族分组契约先冻结再修正——按冻结版本运行时，设计序列作为传递枢纽把全部 148 个设计背景与 48 个天然背景并成一个 196 成员的连通块，违反了设计层单独成层的要求，因此把比对边限制在同层之内并改为报告跨层边，两个版本都有哈希，最终为 102 个组（101 个天然、1 个设计）。冻结队列按收紧后的规则每组取一个背景：64 个背景覆盖 101 个天然组中的 64 个，8,192 个循环、12,977 条去重序列、779,664 个残基，长度 37–72，共 217 个位点对，Kish 有效数为 121.6 个位点对。成对基线 Q 在 64 个组中的 45 个可用，另 19 个不可用（18 个低于声明的支持下限，1 个在 UniRef50 上没有任何命中）；在这 19 个组上成对边界仍未判定，也不替换更弱的估计量。在可用的 45 个组上，Q 在留出同源序列上全部优于自身的独立位点对照（+0.0389 至 +1.0106 nats／计分位点，中位数 +0.3879），这也是它唯一被确立的意义。Q 由先验主导：37–72 残基的结构域对应约 26 万至 102 万个耦合参数，而重加权深度中位数只有 152.9 条有效序列、每位点 2.91 条，且本机保留的语料无法离线加深。此前失败的语料耦合通道（在 22 个可估计实验上 0 次通过正对照）没有在这里被使用或改名复活。

成对边界没有被跨过（[双突变结果](docs/D1_PAIRWISE_EPISTASIS_RESULTS.md)）。名册是预先声明的 33 个无条件检查点，11 个逐残基、3 个字节级、19 个 BPE，三层从不合并；提取计划只携带序列与循环状态索引，入口拒收带测量字段的计划，因此 428,241 次生产前向按构造对标签盲。在 64 个组上，加性零模型的组等权均方误差为 1.15441 [0.92605, 1.39050] kcal²/mol²，序列/profile 参照去掉其中 45.6%，此后测到的一切都小两个数量级。表征交互对比在 33 个检查点、两个支持集上都没有任何一个在三个划分上高于零，面板中位数分别为 −0.00720 与 −0.00344，八个检查点的三个区间全部低于零，其中最负的是蛋白模型。似然交互在 64 个组上有 3 个检查点三个划分均高于零（ProGen3-3B、ProLLaMA、ProtGPT2，+0.00085 至 +0.00126，占加性零模型的 0.07%–0.11%），在加入 Q 的 45 个组上只剩 ProLLaMA 一个（+0.00190 [+0.00020, +0.00384] 至 +0.00299 [+0.00080, +0.00575]）；考虑 33 个检查点、两个支持集、三个划分和十个未校正的对比，一个通过与多重比较一致，不能读作已建立的超越成对增量。真正承载结论的是一阶对照：只加入两个单突变似然差、不加任何交互项，增量比交互对比大 3 至 8 倍，并在三个划分上均高于零的检查点有五个（已发表读数）或三个（污染循环敏感性的保守读数），两者都包含 ProteinGLM-7B-CLM、ProGen2-xlarge 与 ProGen3-3B，且全部是逐残基蛋白模型（已发表 +0.00288 至 +0.00812、保守读数 +0.00349 至 +0.00802 kcal²/mol²），在序列间隔不小于 10 的分层上同样大或更大（+0.00380 至 +0.00989）；在一阶表征差之上再加表征交互，则没有任何检查点在三个划分上被识别。因此这一终点上可复现的模型侧信号来自逐残基蛋白模型的单突变似然分数，而不是四状态交互对比；面板中最大的可复现模型增量也只相当于序列/profile 参照所去掉部分的 1.5%。

两项限制界定上述阴性结果，并须与它一同引用。非线性加性对照在这个终点上偏弱：绝对稳定性跨留出家族的可迁移性很低，其第一阶段在 15 个外层折上的组等权留出加权 R² 中位数为 0.141（范围 −0.172 至 0.262），留出 Spearman 中位数为 0.489（范围 0.181 至 0.604），因此相对它报告的模型增量只校正了跨家族序列/profile 预测能够触及的那一部分，声明过的“提供实测单突变”变体没有执行。表征侧的负增量界定的是被压缩的线性读出，而不是表征本身：四个汇总各投影到 256 维共 1,024 列，在 64 个留出组上拟合，岭回归在 360 次惩罚选择中有 43 次落在最强一档。这一点对读出实验与双突变实验同时成立：两项阴性结果目前都只界定“两个提取深度上的压缩线性读出”。两项已授权的实验正在进行，尚无结果，不能预判其结论：[读出类别扫描](docs/D1_READOUT_CLASS_SWEEP.md)在同一批冻结状态上改变读出类别（非线性随机特征、未压缩全宽、扩展惩罚网格），固定队列、各面板的支持集、三个划分种子、折映射、重采样单位与重采样契约，并在每个单元复现已核验的原类别；另有逐层深度重提取据报告正在进行，本检出尚无其记录——保留数组只钩取每个检查点的两个块，因此不重新前向计算就无法解析更多深度。在两者报告之前，上述近零与阴性结果都只能读作对压缩线性读出的界定，而不是关于表征本身的结论。

再上一层的高阶依赖目前是未判定而不是未检出，而且这一判定现在建立在测量结果上，不再是“数据不足”。MegaScale 那批字节里根本没有可用的组合态——这个零现在已被独立普查确认为机械性的：其中所有三阶及以上的状态都是长度改变的构造，经截断列读出来才显得像高阶。随后该关卡在 ProteinGym 上重启：那里三阶及以上的变体大量存在（217 个实验的 246 万余变体中有 94 万余携带三个及以上替换，其中 20 万余具备完整的低阶支持），真正的瓶颈变成“有没有重复通道”。在位点支持最丰富的实验上找到并资格化了一个真正的通道——同一氨基酸状态的同义核苷酸基因型是独立建库、独立生长、独立测序的成员——在 **598 个位点三元组、76 个位置、Kish 有效 119.6**（与成对队列的 121.6 基本相同）上，三阶可复现成分为 0.0437 [0.0000, 0.0716]（标定适应度单位），而通道间不一致为 0.1583 [0.1398, 0.1776]，比值 **0.276 [0.000, 0.496]**；四阶为 0.0000 [0.0000, 0.0509]，五阶为构造零。决定性的比较在同一个实验内部：其二阶可复现成分确实高于零（0.0596 [0.0232, 0.0800]，比值 0.506 [0.178, 0.742]），而已收录的成对终点在相当的有效支持上比值为 2.78 [2.34, 3.26]。因此这是“一个统计功效充分的实验的标签在阶数上的性质”，不是任何模型的性质：没有读取任何模型量，33 个检查点的名册没有运行、也没有被查看或挑选，预算的 9 至 10 个 H200 小时没有支出。第二个候选实验按测量结果被拒绝而不是被省略：其逐重复计数只覆盖 42,328 个变体中的 18 个、1,954 个完整三阶立方中的 0 个，且没有野生型行。这是本项目中“测量边界而不是模型边界”最干净的一例。高阶依赖原本也在其中，其试点已在终点资格化一步给出“未判定”结论——所测三阶终点没有越过自身的通道间噪声下限，可用支持只有两个独立单位，且未运行任何模型拟合，因此这是关于仪器而不是关于模型的结论。动力学、别构、催化、结合与细胞环境属于后续阶段，除非现有数据使其立刻可做。

污染循环的敏感性分析已完成，并不改变本结论的任何主要部分：在“只去掉 3 个缺状态循环”和“去掉全部 130 个受影响循环”两种读数下，表征交互对比仍然没有任何检查点在三个划分上高于零，似然交互仍是同样的 3 个检查点，加入 Q 后仍只有 ProLLaMA。改变的是两个次级计数：一阶似然差从 5 个检查点降到 3 个，似然交互相对自身一阶对照从 3 个降到 1 个。原因值得一并记录，否则会被误读为结论减弱——两处变化在只去掉 3 个缺状态循环时即出现，来自三个组各失去一个等权位点对（217 个位点对变 214 个），而不是来自其余 127 个循环上 0.0315 kcal/mol 的目标位移；四个受影响的情形都是同一个划分（20260924）的下界跨过零，幅度小于 0.00023 kcal²/mol²，另两个划分仍然可识别、点估计全部为正，其中 ProtGPT2 已发表的该划分下界本就恰为 +0.00000。也就是说，在这个支持集上“三个划分全部可识别”这一合取判据本身是脆的，而不是效应不稳定。

“更长程但仍可加的上下文”这一替代解释已被实现、测量，并且吸收不了这项信号：预先声明并绑定摘要的长程可加对照把五个已判定检查点的一阶增益保留在原值的 0.99 至 1.02 倍，在序列间隔不小于 10 的分层上为 0.96 至 1.01 倍，面板计数前后都是 33 个中的 5 个，间隔 ≥10 的计数由 7 个变为 6 个；有界感受野对照吸收的份额同样可以忽略，因此这项信息既不是局部的，也不是该块所代表的长程可加内容。由于这道关卡按声明只涉及似然、完全没有测量表征对比，它不带暂定标记，是现成结论。同时必须记录它对自身的界定，其形状与失败的 k-mer 对照相同：该长程块自身的贡献并未在三个划分上都高于零（+0.00434 [−0.00391, +0.01268]／+0.00322／−0.00051 kcal²/mol²）；共享基线本就包含全序列组成与全序列二肽差，该块相对基线的增量在构造上本来就小；而且这些块既是可加的又是线性读出的，没有逐位置的非线性响应。所以这项阴性结果说明的是“所实现的那个替代解释吸收不了信号”，而不是“长程可加性已被排除”。该关卡另外报告的一个量属于协议中“序列模型近似”这一估计量，与表型预测不是同一个问题，不可混同：用序列统计对照重建模型自身的一阶分数，背景内秩相关的面板中位数为 +0.1519，加入长程块后升到 +0.1675，而加权 R² 在 −18.4 至 −10.1 之间——对照部分还原了模型分数的次序，完全没有还原其尺度。

本结论的表征交互部分已按复评后的读出选择重算，读数不变（198 个单元、9,801 个汇总节点，零个已判定的符号翻转）；承载本结论的一阶似然结果不在此列，它不随读出类别而变。在 ProteinGym 数据上把高阶依赖扩展检验（“立方体”构型）的工作正在进行，尚无结果；在它报告之前，本主张只覆盖二阶终点，对三阶及以上不作任何方向上的声明。

### 结论四：模型信息部分落在蛋白生物物理上，而接触特异性没有被确立

这项结论有两半，必须分开读：资格化的折叠稳定性终点上，四个以蛋白序列预训练的逐残基检查点的似然增量在三个划分上都高于零，稳定性上的落地因此在一阶上成立；而以接触为条件的结构富集在去掉全局非线性稳定性响应之后就消失了，它的对照侧太薄，读不出任何方向，因此接触特异的交互作用是受测量所限、没有被确立，这不是关于任何模型的阴性结论。

结构侧的证据分两类，强度不同。生成序列相对自身组成扰乱对照的结构预测信号是可支持的，但其许可范围在仪器核对后收窄；而把相互作用信号与实验结构接触联系起来的尝试在终点资格化一步即停止。两者一起说明：能力在“顺序信息可被结构预测器识别”这一层有证据，在“信号落在接触残基对上”这一层还没有可判定的终点。

结构结果的解释首先受测量工具自身资格的限制。结构评价使用独立运行时中的 ESMFold2 单序列预测，其本地证据缺口已完成核对：6,790 条预测行、46 份工作回执与 20 个分析单元全部取回本地，其中 6,762 条状态为 ok、28 条失败、无待定；逐残基数组可复现每行自身的均值与不低于 70 的残基比例（6,762 行全部落在 1e-4 pLDDT 点与 1e-6 之内），13,524 份预测文件摘要全部与磁盘字节相符，重跑 20 个分析单元后 22 个检查点估计的均值与区间两端都复现到 0.000 pLDDT 点。因此不再存在逐行重放缺口，也没有结构数字发生变化；退役的 ESMFold v1 产物仍不与当前结果合并。

被许可的结论随之收窄为同一序列与自身固定组成扰乱的配对差，单行数值不宜精确到一个 pLDDT 点以内：以相同设置与种子重折其中 8 行，平均 CA-pLDDT 差值的中位数为 0.081 点、最大 0.929 点，把复制轴分成 16+16 相对不分块的 32 次采样则相差 0.164 点（中位数）与 0.529 点（最大），二者都比所报告的 +6.115 至 +65.195 点的检查点对比小一到两个数量级。校准只在 19 个报告该对比的检查点中的 2 个（ProLLaMA 与 ZymCTRL）上由其自身天然对照达成，其余 17 个继承这两个面板；因此这些检查点上的正对比说明预测器对残基顺序有响应，而不是已在该检查点自身的序列范围内证明天然序列与扰乱序列可分。核对、修复与重复性测量见[结构测量核对](docs/D1_STRUCTURE_INSTRUMENT_RECONCILIATION.md)。

已报告的天然序列校准包括天然序列及其组成扰乱对照，共 116 次预测，全部完成。天然减扰乱的平均 CA-pLDDT 差为 ProLLaMA 类别集 +54.60 [+47.55, +60.54] 点、ZymCTRL 类别集 +65.19 [+62.75, +67.20] 点，均为 97.5% 类别重采样区间。条件生成主批次完成 2,292/2,320 次预测，28 次失败，请求条件的主要配对完整。这一校准可在所测类别与长度范围内支持解释顺序信号，但不自动覆盖所有无条件模型的长度分布，也不替代按检查点自身类别重做的天然对照。

条件生成的平均配对差为正，仍需进一步查看全部尝试中有多少满足预设事件：平均 CA-pLDDT≥70，且至少 80% 残基各自≥70。按此标准推回全部请求尝试，ZymCTRL 的事件估计为 0.8949，ProLLaMA 为 0.0392–0.0396。后者的范围来自未判定部分的事件界限，不是采样置信区间。这些数量反映预测器置信度，不能解释为实测可折叠性或功能成功率。

以接触为条件的关卡按固定顺序设计：先问实测循环量是否在接触对上富集，再问模型侧预测是否富集。第一部分没有通过，因此第二部分没有运行——在声明的接触定义、匹配与加权下，实测的接触减匹配对照差为 +0.192 kcal/mol [−0.061, +0.412]，未被判定；去掉全局响应后的成对特异成分为 −0.001 kcal/mol [−0.095, +0.075]，对照臂的 Kish 有效位点对数只有 25.1。这是终点的限制而不是关于模型的结论：第二部分从未运行，因此任何检查点都不存在“模型侧接触富集”的数字，也不能把它当成阴性结果引用。三点让这次停止成为“测量边界”而不是一个空洞的零结果。其一，同一支持集上的纯噪声负对照——同一循环在两个蛋白酶通道上的半差，只含标签仪器的不一致而不含相互作用——经同一估计量给出 +0.031 kcal/mol [−0.007, +0.064]，因此这个量级的表观富集单靠通道不一致即可达到，而去掉全局响应后的估计正落在这一带内。其二，结构正对照通过了：用同一比对、同一条链和同一套计算读出的相对可及性，与该位点实测单突变 ddG 的背景内秩相关为 +0.285 [+0.130, +0.434]（43 个背景、324 个位点），埋藏位置比暴露位置多 0.502 kcal/mol 的去稳定，说明编号、链选择与可及性计算确实携带与该实验相关的信息，失效的是对比本身而不是注释。其三，现有的表观差异其实来自全局稳定性响应：接触位点的单突变效应更大，而该终点约一半方差是对加性预测的单调响应，去掉这一响应，差异也随之消失。此外这道关卡完全不使用预测结构——64 个背景全部解析到实验结构条目、野生型全长一致性 100.0%、无一被排除——因此 ESMFold2 收窄后的许可范围与它无关。记录见[接触关卡](docs/D1_GATE_STRUCTURE_CONTACT.md)，量值与限制见审计记录的 F22。

这道关卡的第二部分一旦重新设计到可判定的终点上，其模型侧的表征分量也将适用同一条排序规则：表征层面的判决等读出复评完成后再定。折叠与稳定性作为独立关卡已经给出结果。它与成对实验不同：承载结论三一阶结果的单突变似然信号，当时是作为交互实验内部的**对照**被测的，支持集只有 8,192 个四状态循环、且限定在源研究自己挑选的 217 个位点对上，因此不估计一般的稳定性预测能力；新关卡在支持集大得多、且未按“疑似耦合”挑选的单突变终点上重新资格化这一能力，并用它自己的通道间一致性与噪声下限，而不是沿用循环终点的收录结论。该终点在读取任何模型量之前先被资格化：通道间不一致的标准差为 0.2337 [0.2209, 0.2458] kcal/mol，对应的信号标准差为 0.9777 kcal/mol，共享分量与不一致之比为 3.86 [3.61, 4.13]，而四估计循环终点为 2.78 [2.34, 3.26]。

在 101 个家族组、每组一个背景、共 25,856 个单点替换、5,664 个突变位点的冻结队列上，33 个臂共用同一条基线：**没有任何一个臂的分词描述符通过资格化（0/33）**，因此基线不含任何与臂相关的列，每个种子只有一个数值——0.65115、0.64887、0.65747 kcal²/mol²——臂间差恰好为 0.0。似然量在三个预设划分种子上全部高于零的是 **33 个臂中的 4 个**，低于零的是 0 个：ProGen3-3B（+0.02260 [+0.01333, +0.03144]、+0.01307 [+0.00690, +0.01879]、+0.01335 [+0.00203, +0.02368] kcal²/mol²）、ProteinGLM-7B-CLM（+0.01380、+0.01289、+0.01300）、ProLLaMA 第二阶段（+0.00914、+0.00852、+0.00821）与 ProGen3-112M（+0.00764、+0.00814、+0.00939），**四者都是残基级且以蛋白语料预训练的检查点**。不依赖单个区间的部分是界面分层：11 个残基级臂的三种子均值全为正、中位数 +0.00733 kcal²/mol²；19 个 BPE 臂的中位数为 +0.0000016，其中 10 个为正、0 个可判定；3 个字节级臂为 −0.00009，无一为正。相对于已经消除无效应零模型 49.7%–50.4% 平方误差的合格对照集，这些增量只占剩余误差的 1.2%–3.5%，即可判定而量小。抽取覆盖 33/33 个臂、六个批次全部零失败；被其他任务占卡而先被拒绝的 14 个单元按原计划重发，等待卡空闲，未下调队列阈值也未强解锁定。表征层面这里不写任何判决，等读出类别复评完成后再从保留的全宽状态重算。

**这道关卡最该记下的是对照集很薄。** 五个候选对照中只有“非线性可加全局响应”自身的贡献区间不含零（+0.02529 [+0.01639, +0.03460] kcal²/mol²）；组成（+0.00007 [−0.01645, +0.01589]）、突变位点局部进化剖面（+0.01876 [−0.00883, +0.04521]）及其有界改写（+0.02372 [−0.00536, +0.05190]）都被**弃用**；局部化学按三种子同号规则**保留**，但它自身的区间含零（+0.00390 [−0.00068, +0.00864]）。因此这个终点上的“超出对照”，是超出一个只有一个成员确实有贡献的对照集；许可这些增量的是整个合格集——它自身消除零模型 0.65661 [0.56792, 0.75029] kcal²/mol² 的平方误差——而不是其中任一分块。终点之间的差别值得单独记下：在突变排序锚点上是**约束性**对照的剖面统计（F19），在这个“同一背景内两次稳定性估计之差”的终点上反而完全不成为对照——它传递秩（三个种子 +0.03112 至 +0.04804）而不传递水平（某一个种子上把留出平方误差抬高 1.62082 kcal²/mol²，有界改写在同一种子上为 1.33231）。审计记录把这条并入 L48，作为同一形态的第四个终点实例。

**支持集的条件化是一次被测量过的选择，而不是中性的。** 因野生型没有可用测量而被排除的 40 个背景，其自身替换行中带截断 `dG_ML` 的比例中位数为 0.598（四分位距 0.489–0.809），而保留的 101 个背景为 0.047（0.010–0.128），中位数相差约十三倍，同时与标签无关的描述符差别很小（长度中位数 64 对 59 个残基，平均疏水性 −0.311 对 −0.424）。按野生型可分辨性条件化，会系统性排除测量质量差或高度去稳定的结构域，因此整个队列代表的是**可分辨的稳定性状态**：超出测定窗口的变体按构造不在其中，那一层背景整体缺失，这里的任何数字都不估计强去稳定替换的效应。远缘分层同样只是有界的：净化在 20% 一致性/80% 覆盖下保住 4 个可判定臂中的 3 个、在 20%/50% 下保住全部 4 个，但 101 个野生型之间的比对边数在每个阈值下都落在保持组成的打乱零分布范围内，因此净化只约束对最近可检出训练邻居的依赖，**不认证远缘家族隔离**。终点与源数据自带 `ddG_ML` 列的 0.0419 kcal/mol 残差是自一致性而非准确度——该列由本终点所差分的同一批 `dG_ML` 值导出——准确度的陈述是大五倍的 0.2337 [0.2209, 0.2458] kcal/mol 噪声下限。多重性未作任何调整：许可单元共 66 个（33 个臂 × 2 个模型量），此外还有三个划分种子、两套对照集、两种度量与两个远缘阈值；三种子同向的要求是对同一支持集三次相关重采样的合取，不带名义水平。记录见[稳定性关卡](docs/D1_GATE_STABILITY.md)，量值与限制见审计记录的 F28。

#### 设计蛋白稳定性上的推广

MegaScale 提供了与 ProteinGym 互补的稳定性终点。设计侧包含 130 条野生型序列、40 个设计系列和 110,730 个变体；天然对照包含 266 条野生型序列、124 个簇和 404,114 个变体。先检验模型在天然对照上的测量是否成立，再解释设计侧结果。

在通过天然对照的模型中，设计侧排序均未超过疏水性变化和 BLOSUM62。ProtGPT2 相对连续七残基片段基线（7-mer）的 Spearman 增量为 +0.0516 [+0.0213, +0.0807]，区间为设计系列重采样的 95% 置信区间。这表明模型相对该片段基线仍有有限增益，但更简单的参照已构成更强约束。Galactica、InstructProtein、ProLLaMA 等未通过天然对照的设计侧数字不用于库外能力结论。

设计蛋白中的无检索命中只针对所查询的 UniRef50 快照。ProtGPT2 的旧“精确训练库排除”结论依赖较新快照包含较旧训练快照的假设，而簇数增长不能证明成员集合包含；在完成版本和成员映射核对前，尚不能据此声称排除了训练库。其他检查点的语料关系也有缺口，整序列无命中不排除片段支持。

### 结论五：模型会使用生物学上下文，但跨生物学距离的泛化仍然有限或未判定

两个队列的独立单位都远小于行数：读出队列的 25,728 个突变体只落在 163 个 50% 一致性簇上，成对队列的 8,192 个循环只落在 217 个位点对上（Kish 有效数按拟合所用的加权为 129.2，按准备记录的循环份额加权为 121.6，两种约定各自正确，因此引用时必须说明用的是哪一种），其中 99.98% 位于源研究自己挑选的耦合表上。50% 一致性的单连通簇是保守的留出对照，不是经认证的远缘家族隔离：30% 一致性或 80% 相互覆盖以下的同源检测不到，478 个背景中有 179 个没有 Pfam 标签，且家族隔离不等于排除预训练暴露。因此现有的“跨家族泛化”结论都成立于这一层对照之上，而不是成立于远缘家族之上。

此外，ProteinGym 与 MegaScale 存在实验来源重叠，不能作为两次独立生物学确认；该重叠现已量化——冻结读出队列的 217 个实验中有 64 个来自 Tsuboyama 2023（29.5%），而这一来源提供了成对实验所用的全部 MegaScale 稳定性队列，按支持集分层的完整计数见审计限制 L50。

本节所有关于生物学距离与检索的陈述，都是相对我们建立索引的那一个集合，而那个集合并不是各臂的预训练语料。33 个臂里，1 个声明的语料族正是被索引的那一族（ProtGPT2，UniRef50）；11 个声明了另一种蛋白语料——四个 ProGen2 档位为 UniRef90 加 BFD30，RITA-xl 为 UniRef100，还有 InstructProtein、ProteinGLM-7B-CLM、ProGen2-base、ProGen3-3B 与两个 ProLLaMA 阶段；6 个写着显式的“未声明”哨兵值；15 个声明文本语料。只看 14 个蛋白预训练的臂，1 个声明被索引的语料族，11 个声明另一种蛋白语料，2 个什么也没声明——也就是说，对其中 11 个臂，“我们的索引与它们的语料不一致”是实测事实而不是未知。这比“未声明”更尖锐：语料已知不同、也已知没有对着它分档，比只是没有披露更难处理；一个背景对最佳索引命中低于 70% 一致性，并不因此就远离这些臂所见。任何一个臂都没有被推断语料，因为凭空补一个会把一条假事实写进记录该面板的每一件产物；哨兵值是“该语料未披露”的明确声明，而不是名册里的缺口。两件事必须分开：24 个臂在模块里有规格、9 个没有，但这 9 个全都声明了具名语料，而 6 个哨兵臂全都有规格——所以“没有规格”从来不等于“没有声明语料”。另一头的不对称是：对把蛋白读作文本的那些臂，这条分档是“没有信息”而不是“可以放心”——一条蛋白序列与某个索引条目的一致性，并不说明某个网页语料里有没有它。倒转的检索梯度、以及减少近邻支持后一阶增益仍然成立，都是实测结果；需要看清的是这些结果相对的是一个集合的一个快照。真正的限制因素是模型提供方披露什么，而不是我们索引了什么：把索引做大，只在语料已声明的地方才能让分档更紧，而对未声明的语料，任何索引都解不出来。

按生物学距离分层检验这些增益的检索与记忆关卡已经完成，两个队列的分布也因此第一次有了可核验的记录：稳定性队列的 64 个组按对最近检索同源的一致性分为 <30%／30–70%／70–95%／≥95% 四档，分别为 1／2／12／49 个组，检索到近重复的有 49 个、没有的 15 个；读出锚点的 163 个家族对应 0／5／18／140，检索到近重复的 140 个、没有的 23 个。也就是说两个队列都被高一致性单位饱和，锚点上没有任何一个家族落在 30% 一致性以下。

在这一支持集所能承载的范围内，稳定性上的一阶似然增益通过了分层检验，而且梯度的方向与“检索解释”相反。对在污染过滤支持集上通过判据的三个检查点，增量在“没有检索到近重复”的 15 个组、在 37 个浅深度组、在有效序列数少于十的 11 个组，以及在契约判定为单成员的 36 个组上，三个划分区间全部高于零；点估计在检索支持更稀薄的一侧反而更大——ProteinGLM-7B-CLM 在无近重复一侧为 +0.00877 至 +0.00971，有近重复一侧为 +0.00709 至 +0.00787；ProGen3-3B 为 +0.00573 至 +0.00683 对 +0.00295 至 +0.00335——而在面板层面，最浅深度档有 33 个检查点中的 10 个三个划分全部高于零，整体支持集上只有 5 个，最深档一个也没有。检索或记忆解释预期增益集中在高一致性、深支持的分层并在其余分层消失，实测方向恰好相反。由于这道关卡在该终点上只读似然量，这一结论不带暂定标记。

表征侧的分层估计同样算出来了，但按排序规则不下判决；其中一个区分必须保留：在没有检索到近重复的 23 个家族上，表征残留是“因支持不足而未判定”，不是“消失”——点估计仍为整锚点值的 52% 至 103%，而区间从 0.025–0.028 扩大到 0.059–0.070。约束性的限制要两个方向都说清楚：64 个组中的 49 个、163 个家族中的 140 个都处在 ≥95% 一致性，因此远缘一端在稳定性侧只有 1、2、6 个单位，在锚点侧只有 0 和 5 个——两个方向都未判定，既没有通过也没有被否证，这道关卡并没有把任何一项增益放到远缘同源上检验过。一条可行的路线已经被测量出来：320 个 MGnify 背景中有 186 个（58.1%，Wilson 区间 [52.7%, 63.4%]）低于 70% 一致性，按冻结契约形成 192 个组、其中 105 个完全远缘；代价是那批标签的两个蛋白酶通道差异达 RMS 0.824 kcal/mol（平均偏移 −0.478），而合并 ΔG 的总体标准差为 1.251 kcal/mol，即测量下限约为终点自身离散度的三分之二，因此在该支持集上的任何判决都必须对着那个下限来陈述。最终的外部确认仍未开始：它必须取自既不在 ProteinGym 现有面板、也不属于 Tsuboyama 2023 的数据，否则不构成独立确认。

#### 同源上下文

同源上下文实验在已有目标序列之前加入相关序列，再测量目标的预测损失，不生成新序列。最初实验中的 gpt2-large、ProtGPT2、ProGen2-small/medium 均未显示超出局部重叠解释的同源利用。后续扩面在每个检查点 800 个目标单位、1,024-token 预算下，加入组成匹配的无关序列、打乱同源序列和位置占位对照，并检验最低序列一致性与局部重叠分层。

正式判决比较同源与配对无关上下文，要求归一化损失下降及目标内配对胜率在合并集和低重叠层满足门槛。配对胜率表示同一目标下同源上下文优于无关上下文的比例，平局计半；代码虽将其命名为 pairedAUROC，它并不是全部正负样本交叉比较的 ROC AUC。打乱同源与位置占位结果另行描述，并不属于这个通过门槛。

| 扩面中的判决 | 检查点 | 解释范围 |
| --- | --- | --- |
| 通过配对无关对照及低重叠层门槛 | Galactica-1.3B/6.7B；InstructProtein；Llama-2-7B；ProLLaMA Stage 1/2 | 值得在独立家族上确认的上下文候选；未证明超过全部已计算对照，未定位机制或排除局部复制 |
| 与局部复制对照一致 | Galactica-125M/30B；ProGen2-base/large；ProtGPT3-1.3B；ProGen3-3B（逐条重测确认） | 正的合并表现仍可能由局部重叠解释，不能据此声称更一般的同源利用 |
| 本预算内未识别正增益 | ProGen2-xlarge；ProteinGLM-7B-CLM；RITA-xl；ProGen3-112M（逐条重测确认） | 结论限于本次打包、长度和实际上下文条数，不约束专门接受多序列训练的模型 |

ProGen3-112M 与 3B 的扩面判决曾于 2026-09-23 撤回：旧批量实现把批内行号写入了会进入模型表示的序列编号，改变了输入条件。以逐条计分在同一队列摘要、同样两份各 1,000 单元的冻结计划和同样五个条件下重测后，两个判决都得到确认——112M 的合并配对胜率为 0.48000 [0.44471, 0.51585]，归一化损失下降为 −0.00134 [−0.00329, +0.00059] nats／计分 token；3B 为 0.79875 [0.76875, 0.82825] 与 +0.17825 [+0.16007, +0.19588]，门槛各子句的判定与原先一致。其中一个子量反转且不予恢复：3B 在低一致性、低重叠层（67 个单元、67 个组）的配对胜率为 0.55224 [0.43284, 0.67164]，下界不再高于一半，因此“该层配对胜率高于一半”这一说法保持撤回；该子句原本已因归一化损失项未通过，复合判定不受影响。ProGen3 现只允许逐条计分：其稀疏专家模块把批与位置压平为同一个 token 轴，该版本也没有 float16 以上的核，纠正序列编号后批量与逐条的最大差异仍为 3B 在批量 2 时 0.02464 nats／计分 token、112M 在批量 8 时 0.02796，16 条等长序列之间最大 0.07179，因此计分路径在加载权重之前即拒绝大于一的批量，并把批量记入每份计分产物。单条计分的突变上下文实验及其 profile 增量分析不受此缺陷影响。具体范围、表格与回执见[Stage-46 重测记录](docs/D1_PROGEN3_STAGE46_REMEASUREMENT.md)。

#### 远端同源：一个真正的远端压力测试，仍然受测量所限

远端那一头是受测量所限，而不是阴性，而这个限制现在在测量的两侧都有刻画。一份远端同源支持集在那里放了 100 个家族组，它们的每个成员对所搜索参考快照中最佳命中的一致性都低于 70%；这条分档测的是那个快照里的检索一致性，不是与任何检查点训练语料的距离。它自己的下限在这些组上更低：共享成分与通道不一致之比，在 66 个成员全部检索到 70% 以上同源的组上为 1.745 [1.452, 2.084]，在 100 个都没有的组上为 1.387 [1.039, 1.835]；下降来自更小的共享成分，而不是更大的通道不一致——两个不一致区间大幅重叠，所以不是“距离越远噪声越大”。

这道下限是两个蛋白酶通道之间的系统性差异，不是精度不足。把双通道区间过滤按 1.0、0.5、0.3、0.2 kcal/mol 收紧，比值基本不动，为 1.702、1.688、1.678、1.646；到最紧的 0.1 kcal/mol 反而降到 1.447，而保留的支持量掉了 98.3%。原因在成分里：收紧去掉的是高方差状态，共享成分与不一致几乎按同一比例缩小（效应标准差从 0.7200 降到 0.4503 kcal/mol），通道间偏移在每一档都还在。支持量也从来不是瓶颈：在 0.5 kcal/mol 的过滤下，源文件仍给出 5,754 个背景和 117,489 个精确单突变，是已声明队列 305 与 6,291 的 19 倍，后者受限于一次 320 个背景的抽取，不是可用数据。

另一半证据在近同一性那一层，也就是正对照最应该点亮的地方：似然增量在 33 个检查点上没有一个高于零，只有 3 个在三个划分上都保持正的点估计，而 11 个逐残基接口没有一个做到，其中 9 个第三个划分为负、并且负得比第一个划分正得更多。卡住的是符号，不是区间宽度，而增加家族组不会改变符号。所以扩队是因为对照、不是因为成本被否决：远端那一侧的算术其实成立（两个 ProGen2 档位只需要 55 和 57 个组，实测已有 100 个），但在给这些判决授权的那个对照下，第一个能点亮的检查点是 GPT-2，需要 173 个组，其后两个需要 4,527 和 13,281 个。一个首先在文本检查点上点亮的正对照不能为“蛋白序列信息”作证，在它之下增加远端组只会把一个已经失败的对照包装成远端结果。近端那一层的符号会不会在几百个组上稳定，无法从 66 个组估计，所以这个问题是被关闭而不是被推迟。这个终点上没有读取任何模型侧的量，这是资格检验序列在起作用，不是缺口。这次远端搜索以三种互不相同的方式收尾，不能合并成一句。**本终点止于机制**：把双通道区间规则收紧十倍，共享成分与不一致之比基本不动，所以两个蛋白酶通道报告的是系统性不同的量，任何过滤设置都救不回来。**两份公开数据止于独立单位**，两者都公开、完整、质量良好：多同源 DHFR 发布确有远端序列支持（1,208 个背景中有 484 个对索引集合低于 70% 一致性），但在分组契约下这 1,208 个全部落进同一个家族组，只算一个独立单位，下限是八个；两份 GROQ-seq TEV 发布是“一个终点的两个支持”而不是两次确认（共用同一个建库标识符，9,150 个共享变体标识符上的测量值相关到 r = 0.9643），合计七个家族组。这是实验记录的形状，不是我们仪器的限制。**还有一份大规模 MGnify 衍生汇总数据止于获取，而它的内容根本没有被测量**：终点是对的——可溶丰度的 log2 变化倍数，无量纲，既不与自由能混用也不与生长适应度混用；离散通道的**种类**也是对的——1,050 个完全相同的结构域被放进两个库、其中 825 个在两个库里都被定量，是同一条序列在不同建库语境下独立合成并独立测量，而不是条码折叠。但它没有公开的逐测量发布：数据可得性声明指向一张申请表，正文没有任何仓库编号，补充材料只有图而没有机器可读的表格。所以它的家族结构从未被测量，也因此**不并入“独立单位不足”这一条**——那样会声称一个没人做过的测量。与它一起记下的是一条风险而不是判定：它报告两个库之间存在酸胁迫偏移，而温度胁迫高度一致，这与关闭本终点的正是同一种形状，任何分解都得先检验这一点。

因此可以声称的是窄的那一句：**在已触及的公开存放数据中，没有一份同时提供八个彼此非同源的背景与一个可分辨的生物物理终点、且有公开的逐测量发布。**不能声称的是“不存在这样的数据集”——有一个候选止于获取、内容未被测量，就现有证据看它很可能形状是对的。三个终点上都没有读取任何模型侧的量。还有一条限定的方向与其他几条相反：对宏基因组队列，UniRef50 分档会**高估**远端性而不是高估邻近性，因为有六个臂声明的语料里含有宏基因组序列。

#### 独立确认落在近端

结构域丰度那份独立数据（见结论一）的 96 个家族组里有 95 个完全属于近同一性，没有一个远端组，所以它在近端确认一阶结论，对生物学距离本身不作任何陈述。这条轴上“远”与“近”的分档来自对所搜索参考快照的检索一致性；快照里检索不到近亲并不等于该序列不在任何模型的预训练语料里，这条限制与分档同时引用。

### 结论六：学到的生物学约束不会自动转化为生成能力

生成与评分不能互相代入，这一点已有具体的反向例子：ProLLaMA Stage 1→2 在突变排序上提高 +0.0149 [+0.0036, +0.0274]，而无条件 Pfam 命中从 136/800 降到 36/800，两阶段都有 800 条非空、精确唯一的序列。因此“评分、生成与上下文利用不能归为单一能力轴”不是一个谨慎表述，而是被一个同谱系反例支持的结论（见本节的跨指标证据）。生成侧的能力因此必须用生成自己的终点来衡量，并且按全部尝试计分。

条件生成检验输入标签是否改变生成物所属的蛋白类别。实验使用两个已有的原生接口：ZymCTRL 的酶分类编号（EC）和 ProLLaMA Stage 2 的超家族（superfamily）指令。每个类别、每个条件生成 200 次，在各自接口内比较请求、错配和无条件输入。EC 与超家族的定义、识别覆盖和采样配方不同，因此结果用于评价各自的条件选择性，不构成共同任务上的模型排名。

类别归属由 profile 识别；结构方面则比较生成序列与其自身组成扰乱对照，检验残基顺序是否带来更高的预测置信度。CA-pLDDT 是结构预测器对 Cα 原子位置的置信度分数，配对差以分数点表示。下表同时保留类别选择性、全部请求尝试中的目标命中和近重复组数，以区分选择性、产出与冗余。

| 条件模型 | 类别选择性 | 请求条件的全尝试与非冗余产出 | 生成物的顺序信号 |
| --- | --- | --- | --- |
| ZymCTRL，14 个 EC 类别 | 请求减错配的类别归属率 +0.8822 [+0.7202, +0.9872]，95% 类别重采样区间 | 2,800 次尝试中 2,488 次命中目标 profile，落入 1,688 个近重复组 | 生成减自身组成扰乱的平均 CA-pLDDT 差 +58.12 [+46.64, +64.09] 点，97.5% 类别重采样区间 |
| ProLLaMA Stage 2，15 个 superfamily | 请求减错配 +0.1267 [+0.0527, +0.2360]，95% 类别重采样区间 | 3,000 次尝试中 380 次命中目标 profile，落入 380 个近重复组 | 同一配对差 +6.99 [+3.78, +10.87] 点，97.5% 类别重采样区间 |

两个模型均在请求条件下提高了目标类别归属率，但类别选择性不等于高产率，近重复组数也不等于功能多样性。参考检索在同一批请求输出中分别得到 2,658/2,800 和 390/3,000 次 UniRef50 命中；命中相似度须与该命中的两侧覆盖共同读取，无命中不能证明训练外泛化。完整分母和类别结果见[条件生成记录](docs/D1_GENERATION_BIOLOGY_PREREGISTRATION.md)。

无条件生成进一步检验不提供类别标签时的产出。面板包含 ProGen3-3B 及 17 个扩面检查点，每个保留 800 次尝试。扩面包括 ProtGPT2、ProGen2 五档、ProGen3-112M、ProteinGLM、ProtGPT3、RITA、Galactica 四档、InstructProtein 和 ProLLaMA 两阶段；纯文本、Llama-2 和 ZymCTRL 不进入该协议。模型只接收各自原生的无类别起始格式，不添加 EC、超家族或标题条件。

| 无条件证据 | 当前结果及其边界 |
| --- | --- |
| ProGen3-3B 的完整账本 | 800 次中 501 次由原生接口接受，299 次为预算截断前缀；496 次获任意 Pfam 识别，落入 496 个近重复组。128 对生成／自身扰乱共 256 次结构预测全部完成，平均 CA-pLDDT 配对差 +36.58 [+32.39, +40.77] 点（95% 序列组重采样区间） |
| 扩面结构预测 | 16 个检查点各完成 128 对、256 次预测，其配对差的 95% 序列组重采样区间均在零以上。Galactica-125M 仅有 1 对，不足以报告该区间；它在 800 次中仅产生 9 条非空解码、4 个不同哈希、0 次 Pfam 命中，不能从这一对估计稳定产率 |
| 产出不能只报筛选后样本 | 原生闭合、格式检查接受和非空解码不是同一个事件；截断前缀也可能获得高预测置信度。任何 profile 或结构事件都须同时报全部尝试、完整产物、截断、失败、精确重复和近重复组分母 |
| 参考支持与多样性 | 各检查点均报告命中／无命中、相似度和覆盖；命中者的相似度中位数不能代表全部产物。近重复组数描述序列冗余，不等于功能多样性 |

这些产出依赖所声明的生成程序。当前 400-token 上限在不同 tokenizer 下对应不同残基长度和终止机会，原生终止、格式检查接受及非空解码也是不同事件。截断前缀可能获得较高结构预测置信度，却不能据此计为完整生成物。因而必须保留全部尝试、完整产物、截断、失败和重复的分母，并将同一命中的相似度与覆盖配对报告。完整记录见[ProGen3 生成实验](docs/D1_PROGEN3_GENERATION_PREREGISTRATION.md)与[无条件生成扩面](docs/D1_UNCONDITIONAL_GENERATION_EXPANSION.md)；现有结果不支持任务和计算预算均等的跨模型排名。

把“学到的约束能否产出满足生物学要求的序列”作为独立能力轴来检验的生成与控制关卡已经报告。它不是又一个打分测定：判定在全部尝试、完整产物、截断前缀、失败、精确重复与近重复组的分母上给出，生成物的类别归属本身是一个选择步骤，未被归类的部分一并报告。关卡在冻结生成账本的 20 个单元、16,000 次尝试中的 16,000 次上，把模型与七个只携带所声明序列统计量的生成器配对比较，每个对照按该次尝试的确切长度各出一条序列，因此比较的分母就是账本自身的分母。识别用 HMMER `hmmscan` 按 Pfam-A 发布版自带的 gathering 阈值判定，共 115,736 次查询分 252 个分片，每个分片在读取任何终点之前先以 SHA-256 绑定自己的查询文件；把该仪器重放到生成序列上，16,000 次尝试全部复现冻结账本自身的 profile 归属，0 处不一致。

20 个单元合计：模型获任意策展家族识别 5,156 次、完整结构域 3,995 次；组成扰乱 0 次与 0 次；UniRef50 条件分布在阶数 0、2、4 上各 1/1/1 次与 0/0/0 次；疏水性匹配采样器 0 次与 0 次；语料片段 8,217 次与 4,931 次；作为参照而非对照的整条语料记录 8,282 次与 5,953 次。也就是说，五个通过资格检验的对照里有四个在 16,000 次尝试中最多命中一次，它们与模型之差等于模型自身的命中率（相差不超过一次尝试），不携带该命中率之外的信息；真正起约束作用的对照是同长度的语料片段。

按终点分别判定。在“任意策展家族”上，20 个单元中 1 个超过全部通过资格的生成器（ZymCTRL 的请求条件，相对语料片段 +0.3500 [+0.3090, +0.3925]）；18 个超过全部统计生成器而没有超过语料片段，其中两个的片段对比区间覆盖零（InstructProtein +0.0525 [−0.0088, +0.1099]、ProGen3-3B +0.0337 [−0.0200, +0.0863]），属未判定而非阴性；1 个未在任何生成器之上被检出（Galactica-125M，800 次中 0 次，仅 9 条可检索序列）。在“完整结构域”（一个结构域实例覆盖所归属 profile 自身模型长度的 80% 以上）上，3 个单元超过全部通过资格的生成器（ZymCTRL +0.5363 [+0.4888, +0.5835]、InstructProtein +0.2213 [+0.1625, +0.2804]、ProGen3-3B +0.1950 [+0.1400, +0.2487]），16 个只超过统计生成器，1 个未被检出——更强的生物学要求对面板的划分与较弱的那个并不相同。命中率要对照 profile 采样器自身的上限读，而不是对照 1.000：从 1,000 个 Pfam-A profile 各发射一条序列，同一仪器只认出其中 0.892 [0.871, 0.910] 携带任意家族、0.835 [0.811, 0.857] 具完整结构域（Wilson 95%，1,000 条中 1,000 条）。

结构可信度这一终点不给判定，原因在仪器：19 个报告该对比的检查点中有 17 个（22 个单元中的 18 个）不被许可作出逐检查点的“自然序列区别于自身扰乱”分离陈述，因此没有任何结构单元进入上面两个终点的判定。未能超过语料片段也不说明解码器在语料统计之外一无所有：片段是真实蛋白的子串，按亲缘关系继承策展家族，两种比较回答的是不同问题。区间只覆盖同一检查点在一种解码配置、一条批次种子流下 800 次尝试之间的变异，不覆盖配置、整条种子流或重训练，也未就 20 个单元、2 个终点、7 个对照和两种资格读法作任何多重性调整，因此可辩护的读法是跨单元的模式而不是任一单元的区间。两个条件单元按账本全部十六个类别键分层抽取，与条件生成实验已发布的命中率（分别只采纳十四与十五个类别）不可比，不与之对读。完整表格与限制见[生成与控制关卡](docs/D1_GATE_GENERATIVE_CONTROL.md)。

#### 蛋白适配的收益与代价

Llama-2→ProLLaMA 的训练阶段比较在共享架构和 tokenizer 下追踪行为变化。蛋白突变排序改善伴随文本上下文信息与序列—描述检索下降；指令阶段没有恢复基础模型的文本检索表现。检索实验使用 20,000 条序列—描述记录和 1,000 个候选，并分别在近重复组外与家族外评估。这些结果描述同谱系的适配收益与代价，主体、输入接口和输出头的贡献留待方向二分析。

另一个探索检验了跨模型信息交接：在固定的七类 EC 任务上，以预训练模型作为供体，相对随机初始化且权重固定的供体，其增益区间覆盖零；显式分类器交接优于隐状态软桥。因此，现有实验未建立预训练特异的跨模型迁移，该轮探索已关闭，不因面板扩展而自动重开。

#### 不同能力指标不能归为共同规律

早期跨指标分析完成了 34 个汇总量对，以下保留其历史汇总以说明证据演变；修订稿的主要关联采用下文新增的共同支持集分析。在已选的蛋白与联合检查点中，ProteinGym 的 MODEL−LOOKUP 与任意 Pfam 识别率呈正关联：Spearman 为 +0.7606 [+0.3591, +0.9386]，涉及 18 个检查点；与生成顺序信号的关联为 +0.7296 [+0.3312, +0.9080]，同为 18 个检查点；与全部尝试中结构事件下界的关联为 +0.8015 [+0.4072, +0.9600]，涉及 17 个检查点。系数均无量纲，区间为检查点重采样的 95% 置信区间。现有结果因而不支持预先将“评分与生成分离”设为结论。

这些关联仍属探索性结果。相关检查点并非独立随机样本，实验支持集不同，多个检验也未由上述区间充分处理；有限面板的重采样不代表所有模型的抽样分布。输入值目前由手工维护，须由带指纹的原始产物重建，涉及结构的关联还继承 ESMFold2 的重放缺口。完整描述性输出见[跨指标关联记录](docs/D1_CROSS_MEASURE_ASSOCIATION.md)。

尤其需要区分测量资格与能力大小。上下文识别所用的八区块置信下界最小值，同时反映信号、变异和精度；将不同 tokenizer 的 nats/token 排秩也不能消除单位差异。因此，它与其他指标的合并相关不能形成跨模态能力规律。MODEL−LOOKUP 与 MODEL−BLOSUM62 也只有在实验支持集和基线聚合完全一致时才相差同一常数。条件与无条件生成分别解释，同源上下文配对胜率不作为生成指标。

新增的共同支持集分析直接连接留存的逐实验分数与生成账本，不再以手工维护的全模型汇总作为主要关联依据。在 201 个实验、163 个家族的共同支持上，10 个同时具有生成记录的检查点中，突变排序与全尝试 Pfam 识别率的 Spearman 相关为 +0.7818；仅传播突变家族不确定性的条件 95% 区间为 [+0.6970, +0.8909]，逐一移除谱系后仍为正。这个区间固定生成账本与检查点面板，不包含生成种子或所有模型的总体不确定性。另一方面，15 个检查点的同源上下文配对胜率与识别率关联曾因上述 ProGen3 Stage-46 批处理缺陷撤回，重测后已恢复：合并层 Spearman 为 −0.6107，与原值相同，因为 Spearman 读取秩而两个 ProGen3 检查点在两个轴上的秩都没有变化；低重叠层为 −0.5694，取代原来的 −0.5512；逐一移除谱系后八个系数全部为负，范围分别是 −0.7319 至 −0.3091 与 −0.7167 至 −0.2746。替换流程先从历史输入复现被撤回的系数，复现不成则不报告任何结果，其余十三个检查点沿用其保留的历史胜率均值。这里的重采样单位是检查点，15 个是固定的全部面板而不是抽样，因此不估计跨检查点总体的区间；识别率是 800 次保留尝试中的 profile 命中率，不是功能度量。ProLLaMA 的阶段变化又提供了同谱系的反向例子：Stage 1→2 在原始 217 个实验、174 个家族上提高突变排序 +0.0149 [+0.0036, +0.0274]，但无条件 Pfam 命中从 136/800 降至 36/800，两阶段均有 800 条非空、精确唯一的序列。因此，跨检查点的正趋势与同谱系的反向变化可以同时存在；评分、生成和上下文利用不能归为单一能力轴。具体分母、谱系敏感性及尚未控制的长度与生成预算因素记录在[审计记录](docs/INTERPRETABILITY_TRANSFER_AUDIT.md#0-objective-and-current-programme)与 2026-09-21 的实验日志条目中；当时报告的手稿分析文档不在本检出内，完整名册与来源摘要只留在未纳入版本控制的本地分析归档里。

### 能力图谱而不是固定层级

研究方向已修订为面向机制的能力图谱：要回答的是这些模型确实捕捉到了哪些生物学约束、哪些仍未判定、哪些在合格的测量与对照下未被检出。组织轴线是已知的蛋白质机制与约束，每一项作为一道独立设卡的测定；“未判定”与“未检出”是两种不同的结论，不合并表述——前者指仪器、支持集或读出无法在现有材料上作出判断，后者指合格的测量配合合格的对照把模型侧的量判为不高于零。

组成、k-mer 等局部序列统计、profile、共进化与同源检索支持都是对照和相互竞争的解释类别，不是生物学机制，因此不作为机制关卡出现。此前失败的局部模式块正说明这一点：一个无法跨留出簇泛化、反而降低所增强基线留出相关系数的 k-mer 统计，本来就不是机制，越过它也不能许可任何能力主张。该关卡随后建立了一个通过资格检验的局部对照并已给出判决，这正是这一区分的作用所在：机制问题没有随第一个对照的失败而消失，那次失败被保留为“资格条件为何存在”的理由，而不是关卡的状态。它们同样不构成必须逐级通过的阶梯，而是在支持集允许时各自独立检验的竞争解释；因此交叉对照的结果属于对照资格化，而不是机制判决。

还有一条贯穿全程的排序规则：终点、基线、对照与数据集的资格化工作并行推进到完成，模型侧的量也可以提前提取，但表征层面的判决一律等读出复评（读出类别扫描与逐层深度重提取）完成后再给出，届时各关卡的表征分析会被系统地重新审视并在必要时重算。似然层面的判决不在此列——似然以单个标量特征进入比较，线性读出已接近该特征上的全部函数空间，读出类别的问题几乎不起作用；而四个汇总在两个深度上投影到 1,024 维只是众多压缩选择之一，正是在那里一个零结果可能属于读出而不属于状态。这条规则的意义在于：“模型边界还是测量边界”这一区分不只写在讨论里，而是落实在结果的发布顺序上。同一区分在证据侧也有具体实例，其中最干净的是高阶依赖关卡：在与成对终点相当的有效支持上，三阶终点没有越过标签自身的噪声，而同一实验的二阶成分确实高于零——这是标签在阶数上的性质，不是模型的性质，并且没有读取任何模型量。当结果显示某道关卡的终点、对照或统计程序不充分时就重新设计，不把实验朝预设的能力结构推。各关卡的问题、准入条件、当前状态与记录以[能力图谱计划](docs/D1_CAPABILITY_MAP_PLAN.md)为准；成对实验的设计与估计量纪律仍由其原有的预注册文件承担，全部发现、区间与限制仍以审计记录为准。

方向一已将部分可测效应与接口、基线和数据范围联系起来：评分增益取决于比较参照，原生条件改变类别归属，而生成顺序信号、完整产出与实测功能仍是不同终点。同源上下文和训练阶段实验进一步表明，同一模型的表现会随输入信息与训练阶段变化。上一节的读出与双突变结果又划出了边界：可复现的模型侧优势集中在似然赋值，而冻结表征相对同标签预算的序列与 profile 对照基本不再提供可识别的额外信息。这些结果可以为方向二选择明确的能力及对照，但尚不足以把检查点差异归因于模态或规模，也不能把间接生成证据提升为具体生物机制。

用户授权的五组解释性追加实验现已完成计算和证据核验，具体支持集、区间与产物集中在[追加实验记录](docs/D1_EXPLANATORY_FOLLOWUP.md)。同源上下文提高了 ProLLaMA 两阶段相对无关上下文及无上下文的突变排序，但仍低于 LOOKUP；ProGen3-3B 相对无关上下文的增益则只恢复了部分前缀损伤，同源条件仍低于无上下文。ProteinGLM 的野生型似然提高，而排序变化尚未确定。低局部重叠分析保留这些区别；低同源一致性层在原生与共同支持分析中仅有三至六个家族，区间按规则不予报告。

生成诊断显示 Stage 2 的低识别主要出现于正常终止的完整格式产物，且完整产物较短；重复指标变化不一致，尚未分离长度或重复的因果贡献。[组件实验](docs/D1_COMPONENT_DIAGNOSTIC_RESULTS.md)确认两阶段 embedding 与输出头完全相同，因此取消重复参数配置，并完成 attention／MLP 交换。MLP 交换的平均 Pfam 识别效应为负，但新突变抽样中的阶段收益和组件效应均未被区间确定，不能声称已定位突变收益或能力分离。全部实验完成不等于生物机制已经解释，也不替代独立家族确认。

候选效应仍需独立数据确认及明确的生物学终点。下表仅保留后续确认工作的设计与判定原则，不属于当前执行范围；已完成的追加实验不自动完成这些要求。

| 下一步 | 需要回答的问题与设计 | 判定原则 |
| --- | --- | --- |
| 补齐现有证据 | 补齐 ProGen3 R242 运行回执与实验日志；ESMFold2 的逐行预测、校准记录与运行回执已追回并核对，余下的是按各检查点自身类别与长度范围重做天然对照校准；以固定版本的产物重建主表及关联输入，并核对 ProteinGym／MegaScale 来源重叠 | 校准仍由两个检查点面板继承的结构对比只许可同一序列与自身扰乱的配对差；不以旧预测器替代，不默认重跑整个面板 |
| 独立家族确认 | 选择能区分竞争解释的小面板，预留外部来源且家族隔离的数据，且不取自 ProteinGym 现有面板或 Tsuboyama 2023；预先固定主要效应、最小有意义差异、家族权重、多重比较处理与样本量，优先检验超过 profile 的排序及低重叠上下文候选 | 区间须支持所声明的效应大小；覆盖零为不确定，等效需另设界限。对照失败或固定预算内未通过，则停止相应主张 |
| 生成条件敏感性 | 在开发家族比较作者原生配方与现用配方，固定残基长度和计算预算敏感性设计，并使用独立生成种子；在确认集一次检验 | 报告全部尝试，分别统计完整产物与前缀；不根据确认结果挑选配方、替换检查点或增加指标 |
| 具体序列的实验后果 | 按预定模型、家族、参考距离和计算分数分层抽样，包含预测失败；配天然阳性与统计／检索阴性，以盲法随机化分别测表达、稳定性和任务功能 | 先确认实验对照的动态范围；技术重复不充当独立序列。分别报告全部尝试中的功能事件与定量测量，保留合成／表达失败、未测量项和零增益 |
| 前瞻预测能力 | 在开发家族固定简单预测器，与长度、组成、检索及单指标参照比较；在未见家族和精确对应的实验序列上确认 | 评价家族外误差、校准和增量判别效应；若无可复现增量，撤下预测性主张，保留现有范围内的能力结论 |
| 必要时识别训练的因果效应 | 仅在需要模态因果结论时，匹配架构、tokenizer、初始化及明确的数据／计算暴露，比较文本、蛋白、未配对混合与配对训练，并使用独立训练种子 | 先明确固定计算量还是固定蛋白暴露的问题；不为补全模型表而启动大规模预训练 |

实测功能主张需要对应序列的实验数据，但湿实验并非所有计算结论的形式要求。研究的贡献应来自独立确认的效应及其解释范围；方向二、三尚未完成的机制与知识主张不能扩大方向一的结论。论文图组、完整支持集、失败与撤回记录以及资源安排保留在[方向一研究与发表设计](docs/INTERPRETABILITY_TRANSFER_AUDIT.md#direction-1-publication-design-2026-09-21)，正文可据此依次呈现比较设计、外部预测、原生生成和独立确认。

## 方向二：用现有方法解释能力如何形成、表示和计算

本节按研究决策顺序组织，而不是按工具名称罗列。先校准方法能否测得到，再从扰动响应和注意力模式寻找计算线索；观察性的解释须经得起分词和因果对照，而“能读出来”离“模型真的在用”还很远。稀疏重建与内部特征控制检验解释能否保持或改变行为，最后沿同一训练谱系看训练本身改变了什么。诊断结果与因果证据分别保留，不把前者写成已经解释完整机制。

### 先校准方法是否测得到

| 实验 | 模型 | 数据(格式、示例、数量) | 怎么做 | 结果 | 状态与局限 |
| --- | --- | --- | --- | --- | --- |
| 方法功效与抽样敏感性 | 以 gpt2-large 为正对照，并覆盖合格文本和蛋白模型 | 文本为连续文档，蛋白为 `MKT...` 序列，ZymCTRL 另带 EC 前缀。解释方法复核 500 条 Swiss-Prot、122,671 个计分位置、每个 38 个 triplet 的 top-100 事件，并检查 23,586 个 AlphaFold 模型 | ①测目标自身的因果足迹。②计算门槛解析上限、功效和匹配置换 null。③改为从全语料中按固定随机种子抽样。④用 skip-offset 和独立样本检查选择敏感性 | 单层 MLP 目标的因果足迹约 0.02 nats/token，因此要求恢复 80% 的标准在该目标上不可达；top-100/122,671 的互信息上限只有 0.0066 nats。七类统计都显示蛋白样本比匹配文本对照更敏感 | 已完成。结果给出方法功效和抽样敏感性的正式边界：不能把“测不到”写成模型没有机制 |
| 多残基分词的干预对齐 | ProtGPT2；ZymCTRL、ProGen2-medium 和 gpt2-large 作对照 | 单点突变记录，如 `WT=MKT...; mutation=A42V`：4 个 ProteinGym assay 各抽 200 个突变。另有 128 个合成 8-residue 重复 probe，如两处 `ACDEFGHI` | ①分别 tokenize 野生型和突变体。②检查序列是否等 token 长并且只有一个 token 改变。③把同一残基片段放在不同上下文相位。④检查两处是否仍得到相同 token 序列 | ProtGPT2 只有 47.0%–54.5% 单点突变保持唯一局部对齐；正式 128-probe 重复实验中只有 21.1% 保持 token 一致 | 已完成。其余样本的残基级 patch 没有唯一数学定义；幸存者又是非随机的 BPE 稳定子集。序列级 likelihood 不受此限 |

### 从扰动响应到计算环节

输入扰动的距离衰减先描述输入—输出敏感性；模块消融和内部扰动再定位可能参与的计算环节。响应持续得更远不等于正确利用了远处信息，也不表示传播路径已被识别。

| 实验 | 模型 | 数据(格式、示例、数量) | 怎么做 | 结果 | 状态与局限 |
| --- | --- | --- | --- | --- | --- |
| 输入扰动的距离衰减 | GPT-2、DialoGPT-small、Qwen、Llama 文本模型，以及 ProtGPT2、ProGen2 small/base/medium | 每个模型使用同一固定种子排列上的 5 个不重叠语料切片，每窗 24 条源记录、每个距离带抽 256 个扰动案例；案例不是独立序列，重采样以源记录为单位 | ①按经验一元词频替换源 token，而非定向改写或生物学突变。②用字符／残基声明扰动跨度（5 字符／3 残基）和 9–16、17–32、33–64 距离带，再按各评测集平均每 token 内容量折算，而非逐案例精确对齐。③固定原始预测的最高与次高候选，测扰动前后二者 logit 差的绝对变化。④比较远端／近端达阈比例的衰减比 | 在较严格的 0.50 阈值下，所测蛋白模型组的衰减比较高，即相对各自近端水平下降较慢；在宽松的 0.25 下，文本和蛋白范围重叠，不形成普遍排序或绝对水平结论。阈值是 logit 差变化量，不是概率百分比 | 已完成描述性诊断。字符和残基仍非等价单位，阈值会改变组间差异是否可见；仅适用于指定模型、窗口与扰动定义。不证明有用的长程信息利用、生物学相互作用或完整传播机制 |
| MLP 与注意力的行为贡献 | 六个可测文本模型；ProtGPT2、ZymCTRL、ProGen2 small/base/medium。DialoGPT-small 因资格失败排除 | 连续原生文本或 64–246-residue 蛋白序列。使用从全语料中按固定随机种子抽取的样本；F7 未固定统一记录数 | ①分别消融 MLP 和 attention。②以每个模型自己的上下文信息归一化。③从文件顺序样本改为全语料随机样本。④用第二组独立样本复核资格和结果 | MLP/attention 代价比在文本为 1.491–2.070，在蛋白为 0.530–1.129，范围不重叠；重新抽样后各模型的结果复现到约两位小数 | F7 结果已确定，是当前较稳健的模型差异。该比值不是贡献分解且可以大于 1；尚未完成分词尺度校正 |
| 架构与路由的扰动响应 | Dense：GPT-2、ProtGPT2、ZymCTRL、ProGen2；MoE：ProGen3-112M；同权重 ProLLaMA Stage 1/2 | 原生文本或 Swiss-Prot 64–246 序列。主要实验条件通常各用 128 条记录、每个强度 3 个随机方向；ProGen3 路由诊断另用 256 条、41,155 个残基位置，因此没有统一 n | ①在每层 MLP 输出加入相对范数匹配的各向同性扰动。②比较 Dense/MoE 和同权重双模式。③用随机分组、残基分组和路由边界作控制。④检查结果能否由路由解释 | 蛋白模型内部差异可大于文本—蛋白差异；ProLLaMA 双模式排序在 Stage 1 与 Stage 2 间反转。Dense 与 MoE 都能出现方法失败 | 已完成。排除了“蛋白、MoE 或路由必然更脆弱”等简单解释，但没有识别统一近端机制 |
| 模型规模与扰动响应 | ProGen2 small/medium/large/xlarge，约 1.51 亿、7.65 亿、27.8 亿、64.4 亿参数 | Swiss-Prot 64–246 aa，一字母序列如 `MKT...`。每个规模、每个条件 128 条、3 个扰动方向、1,000 次 bootstrap；主样本与错开抽样起点的独立样本都运行 | ①固定 ProGen2 谱系、逐残基 tokenizer 和评测长度带。②扫描相同的相对 MLP 扰动强度。③比较四个规模。④检验排序是否单调且能否在第二组独立样本中重现 | 除最小扰动点外，各强度和条件均为 xlarge > medium > large > small；medium 始终高于参数更多的 large | 已完成。不存在简单规模律；深度、宽度和参数量共同变化，不能单独识别参数量效应 |

### 精确重复前缀：从注意力模式到因果筛选

| 实验 | 模型 | 数据(格式、示例、数量) | 怎么做 | 结果 | 状态与局限 |
| --- | --- | --- | --- | --- | --- |
| 精确重复前缀的注意力模式普查 | gpt2-large 与 ProtGPT2；两者均为 36 层、宽 1,280、20 个头、50,257 词表和约 7.74 亿参数。另含其他文本模型、ZymCTRL 和 ProGen2 | 每个模型使用 256 个合成重复 probe，复制片段长度为 64 tokens（不是残基），如起始标记后接两份相同的 64-token 片段。自然精确或近似重复文本/蛋白。203,063 个合格 EC 记录中找到 48 个精确重复和 817 个近似重复；同源分层使用 UniRef50 | ①固定架构、规模和 probe 几何，统计 prefix-matching 头并扫描阈值。②加入 tokenizer 单 token 身份候选均分参照。③检查自然精确和近似重复。④穷举头级因果效应并做路径修补。⑤关闭 DIAMOND masking 后按训练语料相似度分层。⑥修正 bootstrap 单位和 collision null 特异性 | 固定 0.10 阈值下，gpt2-large 为 71/720 高分头、ProtGPT2 为 13/720，约 5.46 倍；按各自单 token 身份候选均分参照重读为 5.62 倍。路径修补未证明蛋白通路更间接，同源分层也未定案记忆贡献 | 已完成并收窄。这是匹配对上的描述性注意力模式普查，不是行为能力差异，也不表示完整因果机制已被解释。单头统计不能刻画完整多层计算。身份参照不是多层上下文头的绝对上限。只可陈述匹配对，不是普遍文本—蛋白规律；训练语料中的重复结构仍未匹配，当前没有真正低同源蛋白层，也没有对称文本训练语料控制 |
| 分词粒度对照 | ByGPT5-medium；对照 BPE 文本、逐残基蛋白及 GPT-2/ProtGPT2 | 合成字符或 byte 重复，如 `abc...abc...`；每个模型使用 256 个 probe、复制长度 64，并为每个真 probe 构造无重复 collision null，运行 2,000 次 bootstrap | ①用近似“一字符一 token”的文本模型拆开模态与分词。②分别测量每个模型的同 token 竞争和 identity ceiling。③比较固定阈值 induction 统计。④再比较 PAA 能否找回真正因果头 | ByGPT5 在固定阈值 induction 统计上落入蛋白区间，因此按模型组的模态排序不成立；在 PAA 因果检索中表现得像文本模型，达到自身机会水平的 5.3 倍 | 已完成。分词能制造部分伪差异，但不能解释全部差异；ByGPT5 同时改变架构和训练语料，不能单独识别字母表的因果效应 |
| 注意力筛选的因果准确性 | 文本：GPT-2、Qwen、Llama、ByGPT5；蛋白：ProtGPT2、ZymCTRL、ProGen2 small/base/medium | 单条记录形如 `{native_record, query_position, antecedent_position, predicted_token, matched_decoy_positions}`。不同正式实验和失败审计对“模型 × 独立抽样”的统计口径不同，因此不合并成一个总 n | ①穷举消融，得到每个模型自己的因果 top-k。②用 PAA 分数找回 top-k。③与自身机会水平和只知道层号的基线比较。④排除格式 token 并固定随机种子。⑤在发现集和留出集上检验可能的失败因素 | 所有逐残基蛋白模型只达到或低于自身机会，所有文本模型至少达到 3.6 倍机会；ByGPT5 也通过。ProtGPT2 的表面通过主要由层深携带 | F1–F4 结果已确定，既定模型组已关闭。结论是筛选器在这些蛋白接口上失真，不是模型没有 copy-suppression 机制 |

### 从可读出表示检验到因果使用

| 实验 | 模型 | 数据(格式、示例、数量) | 怎么做 | 结果 | 状态与局限 |
| --- | --- | --- | --- | --- | --- |
| 中间层预测读出 | gpt2-large、ProtGPT2、ZymCTRL、ProGen2 small/base/medium | OpenWebText 连续文档与 Swiss-Prot `MKT...` 序列。蛋白比较包含 64–120 和 64–246 两个长度带；正式审计未统一规定每个模型的独立抽样次数 | ①比较 Logit Lens 与 Tuned Lens。②用有限差分验证输出 Jacobian。③把 Jacobian 秩与输出词表孔径分开。④修复宽带截断。⑤对化学性质投影加入 shuffled/rank-matched null | Tuned Lens 在所有已测模型和两个长度带都更好；扩大到 64–246 后，蛋白侧优势缩小 54%–82%，文本对照增加约 4%。小残基词表的低秩主要是输出孔径 | 已完成，F6 结果已确定。Lens 结果必须同时写长度带；性质投影没有匹配随机分区就不可解释 |
| 同一权重下两种模式的读出深度 | Galactica 1.3B、6.7B 和 30B 的文本与蛋白模式；125M 只作方法参照 | 与各 checkpoint 自己的上下文资格样本使用同一批 128 条蛋白和匹配文本记录，共 20,866 个蛋白目标与 20,864 个文本目标，蛋白渲染为每个计分 token 一个残基 | 在 float32 下用未调优 logit lens 逐层读取模型自己的最终分布，要求输出头重建 KL 不超过 1e-2 nats；以 top-1 一致率的相对跨越深度和跨度归一 KL 深度作为两个统计量，按记录组做 2,000 次 bootstrap，并报告位置上限与朴素渲染控制 | 三个可识别 checkpoint 都显示蛋白模式更晚接近各自的最终预测：蛋白减文本的相对深度差为 1.3B +0.1380 [+0.1331,+0.1434]、6.7B +0.1352 [+0.1292,+0.1412]、30B +0.0263 [+0.0248,+0.0278]；KL 深度差依次为 +0.2808、+0.1947、+0.0946。30B 的两个模式都只在最后一段明显接近最终预测，最深内部网格点的 top-1 一致率为文本 0.3891、蛋白 0.1161 | 已完成，但不进入可引用结论。未调优 lens 不能区分“计算更晚形成”和“中间状态离最终输出基更远”；30B 的两个跨越点都位于最后一个五层宽网格区间，方向可读而幅度未解析。三个 checkpoint 的深度、宽度和参数量共同变化，因此曲线不是规模因果；还缺调优 lens 和独立数据复核 |
| 接触关系的状态与注意力读出 | ZymCTRL、ProGen2 small/base/medium | 接触记录如 `{sequence, i, j, contact, sequence_distance, decoy}`；按 Pfam family 和 k-mer cluster 隔离。正式审计未统一记录正式接触对总数 | ①先修正结构筛选顺序。②训练单位置 hidden-state 控制。③读取原始 attention。④加入距离匹配 decoy 和只含序列间距的控制。⑤比较家族外接触预测 | attention 相对简单控制的优势约 0.03–0.05，相对纯间距控制约 0.03 | 已完成并关闭当前跨位置稀疏特征动机。只适用于 token 与残基明确对齐的模型；ProtGPT2 不可启发式对齐 |
| 生化属性的层间读出 | gpt2、gpt2-large、ProtGPT2、ProGen2 base/medium、ZymCTRL | 400 个 Swiss-Prot 蛋白，64–246 aa；Pfam 分成 188 个 seen 与 187 个 unseen families。每个残基带身份、疏水性、电荷和体积标签 | ①在每层比较残基身份和粗粒度化学属性的读出。②加入 shuffled-property 与 rank-matched 小字母表 null。③要求属性比残基身份更早稳定。④固定 ZymCTRL 的 EC 标签检查泄漏 | 三个无条件蛋白模型中，属性没有比残基身份更早稳定；charge/volume 在各蛋白模型中均为 0/11 层通过。ZymCTRL 的疏水性正结果在固定 EC 标签后从 7/11 降为 0/11 | 已关闭，后续因果阶段未获授权。只否定这一输出 Lens，不说明残差流没有生化信息 |
| 可解码表示与因果使用 | ProtGPT2、ProGen2-medium；ZymCTRL 等无有效因果分母的实验条件被拒绝 | 记录如 `{sequence, Pfam_family}` 或 `{sequence, residue_index, SS3}`。相关流水线覆盖 820 条 Swiss-Prot、44,626 个标注残基 | ①按家族或近重复组切分。②训练线性/MLP Probe。③用 `LEACE` 擦除读出子空间。④重新测模型下一 token loss。⑤比较“可解码强度”和“行为损伤” | ProtGPT2 的 Pfam 可解码技能约 +0.705，但擦除后预测改善约 −0.179 nats；ProGen2-medium 的 SS3 行为依赖只有约 +0.031 nats，远小于可解码强度 | 已完成。“可解码”不等于“模型使用”。ProtGPT2 多残基 BPE 不允许启发式映射到 SS3、burial 等残基标签 |

### 从稀疏表示重建走向行为控制

| 实验 | 模型 | 数据(格式、示例、数量) | 怎么做 | 结果 | 状态与局限 |
| --- | --- | --- | --- | --- | --- |
| 稀疏字典的表示质量 | ProtGPT2、ZymCTRL、ProGen2-medium；联合谱系另测 ProLLaMA | 训练流为 UniRef50、ZymCTRL EC FASTA 或模型原生文本；评测为连续文档或 `MKT...` 序列。不同字典实验的训练 token 数不同，没有统一 n | ①训练不同宽度和预算的字典。②检查死特征、FVU/NMSE 和 Probe 可恢复性。③人工或自动注释特征。④做 steering。⑤检查更宽字典是否改变结论 | 扩宽能减少死特征、改善部分 Probe，却没有带来稳定 steering；FVU、命名性和行为保持的排序不一致 | 已完成方法审计。死特征、低重建误差、可命名性和 Probe 都只能作诊断，不能单独构成机制证据 |
| 稀疏表示的行为保持 | gpt2、gpt2-large；ProtGPT2、ZymCTRL、ProGen3-112M | 原生连续文本或蛋白一字母序列。F11 蛋白主比较使用 64–246 residues、固定 12× 字典宽度；蛋白另扫描约 64–1022 residues 的四个长度带 | ①逐层用字典重建替换 MLP，attention 在受扰残差上重新计算。②在固定序列目标上以原模型和均值消融定义行为恢复，不评估自由生成。③加入闭式仿射、原始神经元和随机扰动基线。④控制深度、训练预算、路由、分词和长度带。⑤不采用连文本正对照也失败的 causal-rank 资格 | 固定 12× 下文本恢复 0.9084–0.9322，蛋白只有 0.0916–0.1641；四个长度带上蛋白仍为 0.0425–0.2724，与文本无重叠。更好重建不保证更多行为恢复 | F11 结果已确定；差异对长度带稳健，但蛋白侧字典宽度稳健性和近端原因仍开放。不能推断蛋白表示必然更稠密 |
| 神经元与匹配扰动对照 | gpt2-large、ProtGPT2、ZymCTRL；另含 ProGen2 规模阶梯和 ProLLaMA 双模式 | 模型原生文本或蛋白评测集。每个子实验的模型和 n 不同，没有统一样本数 | ①把学习字典换成原始 MLP 神经元。②构造与字典误差同范数、同夹角的随机扰动。③比较模型、模态和规模排序。④检查误差方向是否异常 | 原始神经元在部分蛋白模型上胜过学习字典，指向字典训练问题；误差方向并不异常，扰动耐受也不按模态或参数量单调 | 已测但未推广为普遍规律。只能陈述具体模型，不能说蛋白表示必然更稠密或更脆弱 |
| 蛋白生成的特征控制 | ZymCTRL v2；ProGen2-medium 只用于确认干预 hook | 8 类 EC 生成评测：lysozyme、trypsin、ADH、catalase、DNA polymerase、lipase、kinase、carbonic anhydrase；另用 100 个真实 lysozyme 与 100 个长度匹配 UniRef50 蛋白校准指标 | ①按 CLT 直接效应选择特征。②在 L3/L12/L30 做 top-k 流形内 steering。③用 Pfam、CLEAN、ESMFold/Foldseek 评价。④先验证指标能区分真实与随机蛋白。⑤比较 steering 与未干预生成 | 8 类中显著改善为 0/8；真实/随机校准强分离，说明外部指标工作，但 steering 没有优于未干预生成 | 已关闭强 steering、药物设计和湿实验室实验主张。hook 确实移动 logits，因此负结果不能归因于干预路径断开 |

### 沿联合谱系追踪训练带来的变化

方向一的局部上下文关卡留下两条边界，[匹配配对交接记录](docs/D1_MATCHED_PAIR_HANDOFF.md)把它们整理成方向二可以动手的清单；该记录只做设计与刻画，不测量任何模型量。十七项属性全部取自已声明的检查点元数据，并在与任何结果类别关联之前先绑定摘要。似然那条边界被单一属性完全分开——是否有蛋白预训练，边界内 14 个全中、边界外 19 个全不中；另一项"蛋白训练路径"分得一样，因为它是同一件事的另一种写法。表征那条边界除了检查点谱系之外没有任何属性能分开：唯一两侧取值不重叠的是架构族，而它的取值正是"存留者就是 ProGen2、ProGen3 与 ProteinGLM 这几个检查点"这句话换成一个字段，当作解释即是循环。五项属性在 8 个存留者上恒定而在另外 25 个上不恒定（蛋白预训练、残基级 tokenizer、每残基一个 token、无指令或条件阶段、从零开始训练），所以每一项在这个面板上都必要而不充分：其合取在 33 个检查点中的 10 个上成立，成立而不存留的两个例外是 ProtGPT3-1.3B 与 RITA-XL。没有任何序数属性能分开这条边界，而 ProGen2 五档在同一谱系内把方向问题定住了：五档在 12–32 个区块、1,024–4,096 宽度、1.511 亿–64.436 亿参数、两种存储精度与两种语料上全部于三个划分存留，而面板中最深最宽的非存留者（48 区块、7,168 宽的 Galactica-30B 与 64 区块、5,120 宽的 Qwen2.5-32B）什么也识别不出，因此规模、深度、宽度与精度都不是那条轴。

两点结论约束后续工作。表征那条边界没有任何配得好的配对：在保持似然类别不变的 32 个候选中，最好的一个仍有五项属性不同、两项无法判定，而唯一深度与宽度都匹配的候选同时跨过了似然边界。六个候选配对的移植可行性按现有记录全部不成立——四对跨架构族因而张量名与形状无对应；GPT-2-large 与 ProtGPT2 共享 50,257 的词表规模，但词元学自不同语料，同一 id 不指同一词元，把任一方的 embedding 装进另一方形状合法而语义为空；Galactica-1.3B 与 InstructProtein 是否共享初始化在本仓库无任何声明。因此移植工作仍留在 ProLLaMA 谱系上，似然侧只增加 Galactica-1.3B 对 InstructProtein 这一个非 Llama 复现。混杂必须与上面每个计数一并引用：33 个检查点中 8 个存留、面对 17 项相互共变的属性，识别不出任何东西——残基级 tokenizer、每残基一个 token、蛋白语料与从零训练在每个具备它们的检查点上都是同一个发布决定的四个侧面，面板内谱系相关，这些计数是在不匹配名册上的普查计数，未就属性、检查点、终点或划分作任何校正。表征侧的存留格局本身仍是暂定的，格局一变，只依赖表征侧的配对随之失效。

| 实验 | 模型 | 数据(格式、示例、数量) | 怎么做 | 结果 | 状态与局限 |
| --- | --- | --- | --- | --- | --- |
| 蛋白训练能力的组件来源 | Llama-2-7B、ProLLaMA Stage 1 | OpenWebText 文本与 Swiss-Prot 64–246 aa。每种模式每次抽样 128 条，共使用两组互不重叠且固定随机种子的样本 | ①逐张量验证两个 checkpoint 可交换。②双向替换输入 embedding、输出头、完整词汇接口或 Transformer 主体。③在相同上下文信息指标上比较混合模型。④用第二组独立样本复现 | 约 4.69 nats 文本损失主要随主体移动；完整接口只解释正向约 2%、反向约 14%–15%，接口内主要由输出头承担，输入 embedding 约 1% | 已完成并复现。效应不具可加性，且 next-token loss 由输出头直接参数化，不能当作一般组件贡献占比 |
| 蛋白训练后的激活结构 | Llama-2-7B、ProLLaMA Stage 1、Stage 2 | OpenWebText 与 Swiss-Prot。每个模型阶段与模式组合取 1,024 条留出记录、每条最多 64 个位置，共 65,536 个 token 位置；蛋白短记录明确拒绝 | ①读取每层 feed-forward 输出。②以 float64 计算协方差谱、参与率、有效秩和 `r99`。③与 isotropic、坐标独立 null 比较。④比较三阶段的逐层峰 | Stage 1 在 28/28 个内部文本层使谱更集中；蛋白高维区从基础模型的早层峰移到 Stage 1/2 的 17–22 层，Stage 2 不再移动蛋白峰 | 已完成。只覆盖一个谱系和一个模块输出位置；不同谱指标不能合成单一“坍缩倍数”，谱形也不等于行为能力 |
| 蛋白训练前后的表示差异 | Llama-2-7B → ProLLaMA Stage 1；文本和蛋白模式分别运行 | 同输入、同位置、同层的 OpenWebText 与 Swiss-Prot 评测集；使用近重复分组和逐层非退化 `r99` 审核字典基底 | ①先做 offset、正交和线性对齐。②训练独立字典与 Crosscoder。③扫描稀疏权重。④检查拟合充分和特征极化是否同时成立。⑤对单 latent 做匹配随机消融 | 线性对齐仍留残差；Crosscoder 没有同时满足充分拟合与极化。文本侧约 13%–16% live latents 显示超出控制的差异依赖 | 受限候选。差异依赖不等于新增或删除特征；基础模型蛋白行为不可测，蛋白侧因果差分无定义 |
| 同权重文本—蛋白计算子空间 | ProLLaMA Stage 1/2 双模式；Llama-2 作训练前参考 | OpenWebText 与 Swiss-Prot；匹配记录数、每条记录位置数和总计分位置。正式评测包含 3 个 checkpoint、2 个独立语料抽样，共 6 个实验单元 | ①逐层测 occupancy。②消融自身主方向测 necessity。③与随机同维子空间比较 overlap。④做 2×2 cross-mode driveability。⑤把损伤拆成 unigram 和残余上下文部分 | 合成已知答案的 8 项检查全部通过；正式运行中没有任何层得到许可的“分离子空间”或“共享子空间”判决。Llama-2 蛋白模式在多数层可测到非一元频率损伤，但有的层残余占比不足，有的层找不到文本侧必要秩，因此仍不能形成重叠结论 | 已完成，但没有得到共享或分离计算子空间的正式结论。任何子空间重叠都不是生物知识证据 |

**相关工作边界。** ProteinGuide 在固定权重的蛋白生成模型之外训练属性预测器，并在采样时用该预测器重加权离散转移率；论文展示了稳定性、金属结合和 TadA 等属性的生成控制。这证明外部预测器可以有效引导采样，但不等于生成模型内部已经表示或因果使用同一属性，也没有排除同源检索、组成或其他表面捷径。它与本项目的内部特征 steering 不是同一种干预，因此当前作为相关工作引用，不复现为正式实验。

## 方向三：开发新方法判断模型学到了什么

当现有方法不足时，设计新的对照和解释方法，区分语料规律与生物知识。本节按要鉴别的知识问题组织：突变互作、跨模态概念，以及催化、折叠和氨基酸化学与统计替代解释的矛盾。方法资格未通过、结果仍在重组解释内和受限阳性候选都保留在各自问题中；它们不等于已经识别了具体知识。

| 实验 | 模型 | 数据(格式、示例、数量) | 怎么做 | 结果 | 状态与局限 |
| --- | --- | --- | --- | --- | --- |
| 突变互作与语料耦合 | 真实生物阶段未读取模型；gpt2-large、ProtGPT2、ProGen2-medium 只用于合成方法检查 | 双突变行，例如 `A42G:B57V`，且两个 single mutation 必须在同 assay 实测。51 assays、48 identity clusters、7,298 个至少含 10 个 doubles 的位置对；可构建语料耦合者为 23 assays、22 clusters、5,325 对 | ①用两个 singles 建 additive prediction。②用 cross-fitted isotonic regression 去掉全局非线性，得到 specific epistasis。③从语料比对建立 APC 校正耦合并与列置换 null 比较。④只有多数蛋白通过语料正对照才允许读取真实模型 | 语料耦合正对照 0/22 通过，因此模型 epistasis、内部中介和因果阶段都没有运行；合成文本任务可以找回目标 pair | Stage 0 因方法资格未通过而停止。只说明当前 DIAMOND/UniRef50 hit-list 接口不能建立二阶减项，不说明语料或模型没有二阶知识。方向一另行完成的双突变非加性实验使用 plmc 拟合的成对基线，与这一通道无关，也没有把它改名复活 |
| 跨模态概念对齐 | Llama-2-7B 参考；ProLLaMA Stage 1/2 的文本和蛋白模式 | sequence-description pair，例如 `MKT... ↔ “[MASK] catalyses ...”`。Swiss-Prot `fullName + CC FUNCTION`；池 20,000 对、19,385 个不同序列、17 个 concepts；主 eval 为 4,499 条、3,752 组，gallery 1,000 | ①按近重复组和家族切分并遮蔽概念名。②在预注册 layer 22 拟合 mean/Procrustes/affine 对齐。③比较 shuffled、rank、description-only、composition 和 3-mer。④只有蛋白模式清除全部上界才允许文本方向因果注入和外部 Pfam 验证 | Stage 1 蛋白 excess 为 +0.0097，低于 3-mer 的 +0.0272；Stage 2 同向。相同测量在三个 checkpoint 的文本模式通过 | 正式归入重组并停止；Stage 36 因 STOP-35 从未运行。只覆盖一个谱系、一个层和一次独立抽样，且是序列级表示 |
| 催化活性与家族统计的矛盾检验 | ProGen2-small、ProGen2-medium；ProtGPT2 与 ZymCTRL 未通过方法资格 | matched pseudokinase pair，例如 `(RYK_dead, bit-score-matched active kinase)`。原池 461 条；主评测 15 对；另有 8 个 `active_despite_degradation` 记录，与 dead 侧组成 23-group counter-stratum | ①用 20-bit caliper 中和 Pfam bit score。②用催化位点 motif reader 作生物参照。③对相同 anchor 替换计算模型 NLL contrast。④同时比较 1–7-mer、最近活性激酶检索和 composition。⑤检查 motif-degraded active stratum | ProGen2-small AUROC 0.9200、medium 0.8844，但低于 7-mer ceiling 0.9733；counter-stratum 约 0.73，尚未建立超过 motif 参照的能力 | 小规模预算试运行的读出按所测基线归入重组。2026-09-05 的 CPU 核验发现，固定的 16 对双 caliper 候选全部已超过第一道 20-bit 限制，最小差距 21.1 bits，因此交集为零，低于 8 对下限，当前评测关闭。这不排除其他配对设计；ProtGPT2 与 ZymCTRL 未通过方法资格 |
| 折叠与序列统计的矛盾检验 | 最终关闭不依赖模型；ProGen2-small/medium 只有小规模试运行，ProtGPT2/ZymCTRL 被拒绝 | fold triple：`(anchor, composition-near/different-fold C_seq, sequence-far/same-fold C_str)`；199 triples、367 members、346 near-duplicate groups | ①用 DIAMOND 排除可对齐关系。②验证组成与折叠给出反向排序。③计算 fragment、composition 和 Pfam/profile ceiling。④只用 anchor 前缀运行 jackhmmer。⑤若任一统计成员与结构预测同向，就在读取模型结论前关闭 | Profile 找回同折叠伙伴 56/199，找回组成伙伴 0/199，说明 alignment-clean 不等于 profile-clean | 模型无关地归入重组并关闭。F15 是样本构造方法结论，不是模型负结果；只覆盖人类 AlphaFold 子集、staged Swiss-Prot 和这一 triple 构造 |
| 氨基酸化学与语料统计的矛盾检验 | ProGen2-small/base/medium、ZymCTRL；两次确认各有一个独立 ByGPT5-medium 文本控制。GPT-2-large 与 ProtGPT2 被拒绝 | 20 种残基形成 190 个无序对，例如 `(A,V; 理化轴, 语料轴)`。双侧版本为每个蛋白模型固定 4,096 条轴构造记录和两个各 4,096 条的独立确认样本，并排除样本间精确相同或 5-mer containment≥0.5 的记录 | ①替换输入 embedding，测每个残基对造成的 likelihood damage。②以疏水性、电荷、体积和极性定义理化轴。③用相同记录上的 order-7 fragment substitution damage 定义相反的语料轴和匹配上界。④按近重复序列组×被替换符号组做 2,000 次交叉 bootstrap。⑤确定性填充三个跨样本去重的集合，并要求两个独立确认分别通过 | 14/14 正常结束，零失败；四个轴构造和两个文本控制通过，四个蛋白模型的两次确认共八个主读出均为 `CHEMISTRY`。ProGen2 三个 checkpoint 在 tercile/quartile 通过，但 quintile 均回到上界内；ZymCTRL 的模型减上界区间为正，自身化学方向的区间却在两次确认都覆盖零 | 报告结果已完成，但完整重放受限。14 个原始 SHA-256 校验文件、16 个前置交叉引用及运行前后资源记录均已核验；预注册要求的逐记录充分统计未保存，现有产物只能复算 pair-mean 主效应，不能独立重建交叉 bootstrap 区间。结果是针对指定化学描述和 order-7 片段替代解释的 likelihood 敏感性候选，依赖切分和模型；尚不证明下游因果使用、机制或生物知识。GPT-2-large 与 ProtGPT2 的单残基覆盖约 0.5%，两者未通过方法资格 |

## 当前结论与下一步

方向一六项结论的当前状态可以一句话概括：三项在原生行为这一层已支持——一阶能力边界、局部与单残基约束、折叠稳定性上的落地；一项是能力在组合型依赖处减弱，其三阶部分受测量所限；一项在远端受测量所限、在近端有独立确认；一项确立生成与预测是不同的能力。可读出表示这一层**已经没有暂定项**：五个关卡都在各自完整的面板上按复评后的读出选择重算完毕，**没有任何一条判定改变**。程序级的一行必须带着它的分母一起引用：五个关卡、594 个拟合单元、28,611 个被比较的汇总节点，其中 7,122 个在所测幅度下本可跨越零，**零个已判定的符号翻转**。逐关卡（全节点走查）为：折叠稳定性 99 个单元、2,970 个节点；外部确认 99 个单元、1,386 个节点；残基相互作用 198 个单元、9,801 个节点；远端同源 99 个单元、2,574 个节点；局部上下文 99 个单元、11,880 个节点，其中 1,098 个有跨零风险而无一跨越，两条头条增量都在内。局部上下文那一关卡的类别轴全面板上没有任何一个臂三个划分全部存留；另有一批 42 个单元里确有一个臂全部存留，而这一批已经读清楚：其中每一条记录都是按提取深度选择的、没有一条处在已采用的流程上，所以整批都属于深度轴；并且每一条都只有 512 个坐标（同一深度的两个 256 维汇总），而已发表的增量是四个汇总合计 1,024 个坐标。也就是说，深度与容量在这一批里是一起变的，因此这一批里的任何变动都不能单独归因于深度，也都不是已发表增量的变动。那个存留者只是关于“512 坐标、单一深度”这一设计的陈述：ProGen2-large 在局部上下文对照之后的残留上、深度 25、第一个划分，已发表为 +0.007656 [−0.001737, +0.017281]，重算为 +0.010254 [+0.001897, +0.019103]。它不对已发表的增量构成限定，而类别轴全面板没有存留者也并不把它抹掉——两者是不同容量下的不同测量，不是互相矛盾，两条都要看得见。因果使用本文不测量。

完整性计数里有两项在五个关卡上都有记录：只出现在已发表产物一侧的节点为 0、只出现在重算一侧的节点为 0，且 594 个单元全部可达（594/594）。第三项——已发表产物自身的摘要校验——只在三个关卡上执行过：远端同源 33 个臂、外部确认 33 个臂、局部上下文 99 个单元，失败数均为 0；在折叠稳定性与残基相互作用上它**不可执行**，因为这两个关卡的产物树里根本没有绑定逐次拟合的内容摘要。这比“没有记录”是更强的陈述：不从“它们跑完且没有报错”推出任何结论，也不为它们写 0；而且每件产物都记录了自身检查在何时执行，所以“重读时做的检查”与“运行时做的检查”是可分辨的。折叠稳定性另有两个分别登记的复现数字，互不为对方的修正，因为它们是两种走查下的两个量：在四个被许可量上为 1×10⁻¹⁶（与被取代的产物逐位一致），在整条记录上为 1×10⁻¹⁴（最大点为 3.19×10⁻¹⁴），差别来自被许可四量所排除的次级增量与远端分层。引用哪一个都要说明走查范围。

深度对比正在进行，其试点结果按其本身报告，不作更多推广：在折叠稳定性上、同一个划分下，ProGen2-large 在已采用深度 15 为 +0.017944 [+0.001881, +0.033082]，在已采用深度 31 为 +0.018587 [−0.000626, +0.035204]，在被选中的深度 25 反而只有 +0.010453 [−0.004745, +0.024927]，三者的基线预测完全相同（这一点是记录下来的，不是假设的）。被选中的那个深度比两个已采用深度都低，方向与选出它的那个面板相反。这是一个臂、一个划分、未作多重性校正，且与已发表的 1,024 维增量不可比；完整对比会说明它是否成立。两个确认关卡都已报告：独立的结构域丰度数据在域内秩单元上给出 14/33，并重现了按预训练语料划分的接口排序；远端同源在全部 33 个检查点上未判定，且其近端正对照没有点亮。

现有能力结果没有形成统一的规模规律。ProGen2 到 64.4 亿参数仍未越过 profile 通道，ProGen3 的 3B checkpoint 越过了自己的外部 profile 基线；ProLLaMA 的蛋白适配明显改善突变排序，但仍低于 profile 与 BLOSUM62，同时付出显著文本代价。原生条件接口能够把生成推向请求类别，但没有排除训练分布内检索，也不证明功能；最初 F16 的同源上下文增益未超出局部复制解释；ProGen3 两档的扩面判决经逐条重测已确认，112M 在本预算内未识别增益，3B 与局部复制及局部重叠一致，但其中“低重叠层配对胜率高于一半”这一子结论不予恢复；扩面中 Galactica-1.3B/6.7B、InstructProtein、Llama-2-7B 与 ProLLaMA 两阶段在所测预算下通过配对无关对照及低重叠层门槛，仍需独立家族确认；该门槛没有检验全部已计算对照，不能据此排除局部复制或定位机制。不同谱系使用不同语料、分析集和评分方向，不能据此比较“3B 与 6.44B 谁更强”，也不能把差异归因于参数量。方向二的同权重读出显示 Galactica 三个可识别 checkpoint 均为蛋白模式更晚接近最终预测，但差值在 30B 明显缩小；未调优 lens 和末端粗网格仍不允许把它解释为计算深度或规模规律。

隐状态桥接实验 EXP-R2-234–239 已关闭或停止：在固定 EC-7 任务上，预训练供体模型相对随机初始化且权重固定的对照，区间覆盖零，未确立预训练特异增益；普通显式分类器交接优于软桥；该轮未扩大规模。这不是能力排名，也不是新方法，不改变 Galactica tuned lens、F11 与 D3 的下一步顺序。

| 结论 | 当前判定，按四类结论的词汇 | 该判定不许可什么 |
| --- | --- | --- |
| 结论一：一阶能力边界 | **已支持**，在原生行为这一层：在指定的序列基线之上，以蛋白序列预训练的检查点越过，以文本语料预训练的没有；同一条排序在独立的结构域丰度数据、一个不同的被测量上重现（域内秩单元 14/33，平方误差单元 2/33，均为可识别而小） | 不是机制；不同被测量之间是跨量佐证而不是重复测量；读出复评只把可识别的数量往上移，不能据此读成表征层的结论 |
| 结论一附带的校准 | 可读出表示这一层，**已无暂定项，但仍是读出条件下的读数**：冻结状态的拟合读出在 99 个检查点×划分单元中的 69 个上优于原生似然；但相对同标签预算的监督基线，33 个无条件检查点中只有 3 个可识别、30 个没有 | 不能读成蛋白特异的表征内容：同一现象在 14 个字面氨基酸串文本检查点的全部 42 个单元上同样出现。复评的结论是广度上升而幅度几乎不动 |
| 结论二：局部与单残基约束 | **已支持**，在原生行为这一层：在本项目测得最强的对照之上，原生似然的增量在 33 个检查点中的 14 个上三个划分全部可识别，而这 14 个恰好是全部以蛋白序列预训练的检查点 | 不指出是语料的哪一种差异在起作用，也不给机制；同一对照上的表征残留（8/33）属于可读出表示这一层，仍为暂定 |
| 结论三：组合型残基依赖 | 减弱：单突变似然信号在 5 个检查点上可识别；四状态循环上的表征交互对比在 33 个检查点上无一跨过，属**在合格测量下未检出**（以该读出类别为条件）；三阶依赖属**受测量所限** | “未检出”不能读成信息不存在，只说明在该读出类别与标签预算下取不到；三阶不是关于任何模型的阴性结论 |
| 结论四：生物物理落地 | 折叠稳定性**已支持**，在原生行为这一层：4/33 可识别，全部是逐残基的蛋白预训练检查点，无一低于零，增量为对照集剩余平方误差的百分之几；以接触为条件的结构富集属**受测量所限**，接触特异的交互作用没有被确立 | 接触那一半不是关于任何模型的阴性结论；可识别而小的增量不等于模型解释了该被测量的多大部分 |
| 结论五：生物学距离 | 上下文使用部分成立；一阶增益在减少近邻支持后仍然成立，梯度方向与检索解释相反；远端同源终点属**受测量所限**，限制在两侧都已刻画（两个蛋白酶通道系统性不一致，且近端正对照不点亮）；独立确认落在近端 | 远近分档测的是被索引参考快照里的检索一致性，不是与任何检查点训练语料的距离（33 个臂里 1 个声明被索引的语料族，11 个声明另一种蛋白语料，6 个显式未声明，15 个文本语料；14 个蛋白预训练臂里 11 个的语料已知与索引不同）；对宏基因组队列该分档反而高估远端性；远端**未判定**不是阴性结果；可声称的只是“在已触及的公开存放数据中没有一份同时具备八个非同源背景与可分辨生物物理终点且有公开逐测量发布”，不能声称这样的数据集不存在 |
| 结论六：生成能力 | 不自动转化：原生条件接口把生成推向请求类别，但相对长度匹配的真实语料片段，完整结构域终点上只有 3/20 个单元跨过；同谱系反例（突变排序上升而无条件识别下降）确立生成与预测是不同能力 | profile 命中与结构预测都不等于对应序列的实测功能；不能用排序表现代替生成表现，也不能反过来 |
| 三个层次不能互相代入 | 原生行为（冻结似然赋值）已有可下的判定；可读出表示（拟合读出）的五个关卡重算全部返回且判定不变，但读数仍以该读出类别为条件；因果使用（干预）本文不测量，只有方向二的试点触及 | 任何一层的结论都不能移到另一层；把三者合并会让每一条结论都被读得过强 |
| 模型比较 | 分词、原生输入输出接口、训练阶段、语料、架构和抽样评测都会显著影响跨模态比较；至今没有一条简单的模态规律或规模规律能替代这些因素 |
| 方法审计 | 多种文本可解释性方法在蛋白模型上存在已测得的失真或适用边界；可解码、可重建、可命名或注意力清晰都不等于因果忠实 |
| 生物信息的行为证据 | 条件家族特征提供生成行为证据；结构汇总报告正的序列顺序信号，其逐行与回执证据已核对，但校准只在 19 个报告该对比的检查点中的 2 个上自身达成、其余 17 个继承，许可范围为同一序列与自身扰乱的配对差。正信号与高有效产出仍须分开 |
| 具体规律与机制 | 方向二、三尚未完成因果使用与独立生物验证，也未建立新的生物学规律。其方法资格规则不是知识存在的普遍定义 |
| 不能声称 | 不能从有限阳性断言普遍理解，也不能从阴性断言没有知识或只在记忆。经过校准的生成表现可提供强度不同的间接证据，但 profile 命中、结构预测和低参考相似均不等于对应序列实测功能，也不识别具体知识或机制 |
| 方向一、二下一步 | 方向一的解释性追加实验已完成，后续重点是独立确认与机制验证；同时保留 ProGen3 DMS 来源与运行凭据的核对要求，以及按检查点自身类别重做 ESMFold2 天然对照校准的要求。方向二的既定候选仍是 Galactica-1.3B 的等容量 tuned lens、独立分组留出和末端逐层测量，仍有差异才做因果 patch/消融及 6.7B 复核；F11 则直接检验干净激活拟合与顺序替换评分的分布差异。追加实验不自动完成这些机制检验。EXP-R2-234–239 已关闭，不插入第四个研究方向 |
| 方向三下一步 | 字母表—化学实验已有双确认候选，但须同时保留切分敏感性、ZymCTRL 自身效应不确定和逐记录统计缺失的限制。优先选择自身效应区间为正的 ProGen2 checkpoint 与一个已建立的下游能力，用现有干预验证因果选择性、未见家族泛化和独立实验标签；若只造成通用 likelihood 损伤，或已有基线仍能解释，就停止该设计。只有明确的现有方法失败才支持开发新方法；未来记录须保存可重放的充分统计。方向三的重组上界是针对已实现替代解释的资格规则，不是“模型没有生物知识”的证明 |
