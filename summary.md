# InterpretabilityTransfer 研究方向与当前结论

**更新日期：** 2026-08-19

本项目比较纯文本、纯蛋白质和语言–蛋白质联合生成模型，研究它们为什么不同，以及从语言模型借来的可解释性方法在蛋白质模型上是否仍然可信。最终目标是判断蛋白质模型学到了哪些生物知识；在测量方法通过严格验证之前，不据此提出新的生物学结论。

详细证据、限制和撤回以 [`docs/INTERPRETABILITY_TRANSFER_AUDIT.md`](docs/INTERPRETABILITY_TRANSFER_AUDIT.md) 为准；最新但尚未晋升的实验记录见 [`docs/EXPERIMENT_LOG.md`](docs/EXPERIMENT_LOG.md)。

## 研究路线

三个 Objective 不是并列选项，而是一条连续的证据链：

1. **先比较模型本身。** 分析分词、训练语料、训练阶段和架构如何影响模型行为与计算。
2. **再审计可解释性方法。** 检查方法测到的究竟是模型机制，还是分词、输出接口、样本选择或方法本身造成的假象。
3. **最后检验生物知识。** 只有通过因果、检索感知和独立生物数据验证的方法，才可用于判断模型学到了什么，并进一步提出新的生物学假设。

Objective 1 和 2 不能单独证明模型“没有生物知识”；它们负责建立 Objective 3 所需的可信比较和测量工具。

## Objective 1：模型为什么不同

当前结果表明，分词和输出接口可以解释一部分文本–蛋白质差异，也能制造看似显著的伪差异。例如，多残基分词会使某些残基位置无法一一对应，因此位置级干预可能没有明确定义。但分词不能解释全部差异：一些符号粒度相近的文本模型，在不同测量上仍表现为文本模型。

匹配训练谱系的结果还表明，蛋白质继续预训练不仅改变蛋白质能力，也会影响原有文本能力；这种变化涉及模型主体和输出头，不能简单归因于输入词表。现有规模实验也没有支持“模型越大越稳定”之类的普遍结论。下一步仍需利用同一权重的双模式模型，进一步分离模态、训练阶段和接口的影响。

## Objective 2：可解释性方法是否可信

目前最明确的成果来自方法审计。探针能读出信息，不等于模型会使用这些信息；重建良好、注意力图清晰或某个特征容易命名，也都不能单独证明机制。

一些注意力筛选方法在文本模型上能找到重要组件，在已测蛋白质模型上却不能稳定找到真正具有因果作用的组件。稀疏字典和 Transcoder 在蛋白质模型上也更难保持原模型行为，但近端原因尚未确定。Model Diffing 和 Crosscoder 可以测量 checkpoint 之间的表征或计算差异，目前却不足以证明训练引入或删除了某种生物特征。

这些结果说明，可解释性方法必须先通过接口检查、正对照、行为保持和因果验证，不能把文本模型上的成功直接外推到蛋白质模型。

## Objective 3：模型是否学到了生物知识

这里的核心困难是区分三种情况：模型真正利用了生物规律，模型检索或重组了训练语料，以及模型只依赖片段、组成等表面统计。项目因此把片段统计、profile-HMM 和序列耦合等语料统计作为“重组上界”，并要求测试集让语料统计与生物学知识给出相反预测。

已有的适应度预测、设计蛋白、跨模态概念对齐、假激酶和折叠矛盾集实验，都没有完成“超越重组上界—因果使用—独立生物验证”这条完整链条。有些结果没有超过片段或 profile 统计上界，有些候选队列本身无法区分统计与生物学假设。因此相关路线按预先规则停止。这表示当前实验没有识别出足够强的知识证据，不等于证明模型没有生物知识，也不等于证明模型内部只是在记忆。

目前仍没有任何方法通过完整验证，也没有建立新的生物学知识。较有希望的后续方向，是构造真正的矛盾集，例如检验模型把氨基酸当作具有电荷、疏水性和体积的化学实体，还是只当作由语料共现定义的符号。即使模型先超过语料统计，这也只能成为候选证据；还必须通过内部因果干预、未见家族泛化和独立实验验证。

## 当前结论

本项目目前最可靠的结论不是“蛋白质模型有或没有生物知识”，而是：

- 模型基础和评价接口会显著影响跨模态比较；
- 多种常用可解释性方法在蛋白质模型上存在已测得的失真或适用边界；
- 现有实验尚未把生物知识与检索、语料统计和简单理化规律可靠分开；
- 只有先证明方法能够忠实测量已知生物机制，才适合用它寻找未知机制。

因此，现阶段的主要成果是明确了哪些结论可以相信、哪些结论应撤回，以及下一轮实验必须跨过什么门槛。
