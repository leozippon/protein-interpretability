# InterpretabilityTransfer 研究方向与进展

本文只保留理解研究问题、关键实验进度和结论所必需的信息；模型形状、单次运行数值和完整证据链等细节不在这里展开。冻结结论、限制、撤回和当前计划以 [`docs/INTERPRETABILITY_TRANSFER_AUDIT.md`](docs/INTERPRETABILITY_TRANSFER_AUDIT.md) 为准；尚未晋升的最新实验进度见 [`docs/EXPERIMENT_LOG.md`](docs/EXPERIMENT_LOG.md)，并在本文标为初步结果。

**更新日期：** 2026-08-12

## Research Objective

1. **从基础因素比较模型体系。** 分析纯文本、纯蛋白质和语言–蛋白质联合生成模型在分词、训练语料和模型架构上的差异。
2. **使用并审计可解释性方法。** 用可解释性方法研究模型行为、表征和因果计算，同时检查方法本身是否可信。
3. **提出并验证用于生物知识研究的方法。** 设计能够区分模型知识、语料检索和表面相关的可解释性方法；通过因果、检索和独立生物数据验证后，再用它提出和检验新的生物学假设。

表中的百分比表示关键验证环节的粗略完成度，不代表结论可信度、计算量或论文完成度。撤回一个站不住的结论，或让一个候选方法在预注册关卡处停下，同样算验证进度。

## Objective 1：从基础因素比较模型体系

| 编号 | 假设 | 关键验证 | 实验进度 | 结论 |
|---|---|---|---|---|
| R1.1 分词与输出接口 | 分词、输入格式和输出空间能解释部分、但不是全部文本与蛋白质差异。 | 比较不同分词方式的纯模型，并检查联合模型两种模式的渲染、输出和可测量性。 | 70%。纯模型对照已完成；五个联合 checkpoint 完成资格检查，其中一条谱系两种模式都可测量；面板新增一个字节级文本模型，用作符号粒度对照。 | 小残基词表会限制输出空间，错误渲染也足以制造虚假差异。字母表大小现在可以定量解释一部分模态差距：同一个固定注意力阈值，对逐残基模型要的是它可达上限的四成多，对 BPE 文本模型只要一成出头，字节级文本模型因此在该统计上落入蛋白质区间；但同一个模型在检索类统计上仍是文本表现，分词解释不了全部差异。还有一条结构性事实：多残基 BPE 按上下文切分，相同残基内容未必切成相同 token，位置级干预因此在部分样本上没有定义，而留下来的样本不是随机子集。联合模型的蛋白质模式多采用多残基分词，其数值不能与逐残基模型直接比较。 |
| R1.2 训练语料与知识来源 | 训练语料和适配阶段会改变蛋白质能力的来源，并可能牺牲原有文本能力。 | 将模型表现与训练语料同源检索比较，并沿匹配训练谱系观察两种模式何时出现或消失。 | 70%。纯蛋白质模型的检索界、联合模型匹配谱系和组件交换已完成，组件交换在第二次独立抽样上完整重现；天然蛋白质的家族外和时间切分已测明在该基准上不可达，改用按构造不相交的设计序列参照集（见 R3.4）。 | 当前适应度接口没有证明三个纯蛋白质模型超越训练语料检索。联合模型的主要文本代价出现在蛋白质继续预训练阶段，且主要随 Transformer 主体移动，词表接口内的份额几乎都在输出头；蛋白质能力由主体和输出头共同承载，不需要重训的输入嵌入。原先"蛋白质能力被破坏"的读法已按该单元自身的反向对照收窄：换回基础输出头只是让这条臂在这个估计量上不可测量，方向性适应仍有残留。两次抽样是一次比较而不是一个分布，不能据此给区间。 |
| R1.3 模型架构与计算路径 | 架构和模态可能改变 MLP、注意力和路由的分工。 | 比较形状匹配的文本/蛋白质模型、Dense/MoE 蛋白质模型，以及合格联合模型的两种模式。 | 70%。纯模型的主要架构与路由对照，以及联合模型的双模式扰动和组件交换均已完成；蛋白质侧四级规模阶梯已经跑完，并在一次内容掩码修正后重测。 | MoE 架构和路由不是模型替换失败的单独原因。规模阶梯不支持任何规模结论：扰动恢复的排序在三组条件和两次抽样下稳定，却不随参数量单调，中间两级与规模相反；各级的深度和宽度还同时变化，即使排序干净也分离不出规模本身。扰动鲁棒性仍取决于具体 checkpoint 和训练阶段，不能概括为蛋白质模态的固有属性。 |

## Objective 2：使用并审计可解释性方法

| 编号 | 假设 | 关键验证 | 实验进度 | 结论 |
|---|---|---|---|---|
| R2.1 局部表征与干预 | Lens、Probe、Erasure 和 Activation Patching 在接口匹配后可以回答可比的局部问题。 | 分开检查信息能否被解码、行为是否依赖它，以及干预是否损害无关行为。 | 45%。纯模型完成了部分实验，并新增两条直接作用于这些方法的限制；联合模型尚未进入方法实验。 | 这些方法能提供局部证据，但稳定解码不等于因果依赖，也不能单独构成完整机制解释。任何把输出分布粗粒度化的读出都必然比原分布更早"解析"；在二十个符号的字母表上，随机属性零分布的极端分位数本身就很高，所以没有匹配零分布的粗粒度读数不是弱证据，而是无法解释。位置级干预则要先确认两次运行的分词确实对齐（见 R1.1）。 |
| R2.2 注意力与 PAA | 注意力模式或 PAA 选择器能够找回真正影响输出的注意力头。 | 用穷举路径修补建立因果标签，并与随机机会和只使用层位置的简单选择器比较。 | 80%。纯文本和纯蛋白质模型的主要因果审计已完成；字母表碰撞对照按预注册规则在十五个臂上跑完，面板级问题就此结案。 | PAA 在文本模型上有效，但在已测逐残基蛋白质模型上不可靠；蛋白质模型仍存在有因果作用的归纳注意力头。架构匹配对的差距成立，而且是反混淆的：该蛋白质模型的碰撞上限更高，碰撞效应预测的方向与观测相反。面板级模态排序已永久撤回——一个字节级文本模型在同一统计上落入蛋白质区间。一并撤回的还有"显著高于自身零分布"式的归纳头计数：针对匹配零分布的显著性是检测判据，而计数归纳头需要效应量判据。 |
| R2.3 稀疏字典、Transcoder 与模型替换 | 如果稀疏字典保留了模型计算，逐层替换后应保持原模型行为。 | 在可比预算下替换 MLP，并用 Dense/MoE、分词、线性基线和不同序列区间作控制。 | 80%。五个纯模型的主要行为对照、稳健性检查和神经元基线已完成，参照臂的估计量恒等式已由实测验证；联合 checkpoint 的首次替换测量已完成。 | 文本模型的替换忠实度显著高于蛋白质模型，且差距不是归一化造成的；已有单因素解释均不足，近端原因仍未确定。最新控制初步指向蛋白质模型对 MLP 输出扰动异常敏感，而稀疏字典在该量级上恰好就是这样一种扰动。联合 checkpoint 上四个格子的行为恢复全部为负，三个候选解释未被分离；后续深度扫描初步排除了"单块重建不足"，但没有建立逐层累积。这是一次记录在案的测量，不是关于联合模型可替换性的结论。联合 checkpoint 的组件网格只有一个族，因此只能与同一 checkpoint 的另一模式对读，不能与稠密模型对读。 |
| R2.4 同输入 Model Diffing 与 Crosscoder | Model Diffing 能够区分训练阶段引入、删除或保留的稳定特征，Crosscoder 能在简单对齐不足时表示这些差异。 | 在匹配 checkpoint 上使用完全相同的输入，先比较原始差异、线性对齐和独立字典，必要时再训练 Crosscoder，并用干预验证模型特有特征。 | 25%。两种模式的原始差异与线性对齐基线都已跑通；独立字典、Crosscoder 和干预验证尚未开始。 | 尚无训练阶段特有特征或方法忠实度结论：两种模式的读数都只是表征层面的，基础模型的蛋白质模式仍不可行为测量，只能作为适配前的表征参照。蛋白质模式此前一直被划分保护拒绝，而该保护是对的——在蛋白质语料上按记录留出并不真正留出，一个池里四成多的留出记录在训练侧仍有高同一性亲缘，而匹配的文本对照经核对确实干净。这把该限制定位到评价接口，而不是方法本身。 |
| R2.5 测量方法审计 | 先确认任务可测，再检查行为和因果忠实度，可以区分真实失败与评价问题。 | 依次检查接口、可测量性、统计功效、分母、行为保持、因果目标和独立数据稳定性。 | 85%。流程已用于纯模型实验、五个联合 checkpoint 的资格检查、联合模型的替换与 diffing 单元，以及两个 Objective 3 候选方法。 | 这套流程修正或撤回了多个表面结论，并在联合模型上先发现渲染和模态坍塌问题；今日又新增四条限制，并把其中一条明确定位到评价接口。它也让两个 Objective 3 候选各自在事前关卡处停下，其中一个的算力代价为零。重建误差、探针准确率、注意力模式或单一恢复率都不能独自证明机制；这是目前最成熟的方法学成果。 |

## Objective 3：提出并验证用于生物知识研究的方法

| 编号 | 假设 | 关键验证 | 实验进度 | 结论 |
|---|---|---|---|---|
| R3.1 概念对齐 Lens | 将不可比的 token 输出投影到预定义概念空间，可以恢复有意义的比较。 | 在蛋白质内部使用生物属性，跨模态只使用两侧都有定义的概念，并加入随机概念、因果干预和未见家族验证。 | 60%，已关闭。方法已实现并在六个臂上跑完第一阶段：随机属性零分布、秩匹配零分布和家族划分对照都已完成；因果干预阶段因预注册关卡先行触发而从未授权。 | 前提被证伪。在所有无条件蛋白质臂上，生化属性都比残基身份更晚解析，"孔径增益"最大的反而是文本对照；唯一为正的蛋白质臂靠的是它自己的 EC 条件标签，固定标签后归零。这是测得的否定而非仪器失败：最终层正对照在全部臂–概念格子上通过。它留下的是一条方法限制（见 R2.1），不是一个可用的方法。 |
| R3.2 语言中介的因果概念对齐 | 文本与蛋白质模式可能对同一生物概念形成可校准的共同表征或因果响应。 | 使用真实序列–描述配对，在序列或预定义概念层次测试检索、线性和正交对齐，必要时再训练小型 Adapter，并用概念干预验证。 | 0%。双模式 checkpoint 已完成资格检查，对齐实验尚未开始。 | 尚无共享概念或跨模态激活对齐结论；文本和蛋白质 token 不能直接配对。 |
| R3.3 跨模态因果电路翻译 | 对同一生物概念分别建立可信的蛋白质和语言 Circuit 后，两者的节点和因果边可能形成可验证的映射。 | 先让两侧 Circuit 分别通过节点和路径干预，再检查蛋白质 → 语言 → 蛋白质往返一致性，并用未见家族和独立生物证据验证映射后的预测。 | 0%。两侧 Circuit 忠实度和概念对齐等前置条件尚未满足。 | 尚无 Circuit 翻译结果。 |
| R3.4 生物知识验证与发现 | 通过审计的方法可以区分模型编码的生物知识与同源检索或表面相关，并据此产生可检验的新假设。 | 使用未见家族、同源检索、传统基线和外部表型或功能证据验证已知机制；只有验证通过后，才预注册并检验新的机制预测。 | 45%。纯蛋白质模型的检索界已完成；为越过一阶检索界设计的二阶方案已预注册并在自身第一道关卡处停止；随后一个语料不相交的参照集被建立并通过证书验证——同一个实验文件里 148 条从头设计中有 132 条检不到语料同源，而同一文件、同一实验技术的天然结构域对照有 99.4% 检得到，在设计自身所在的长度区间内仍有 96.8%，所以这种缺失不是短序列比对不上——表型预测实验已按事前冻结的规则在这个参照集上跑完。检索这一环从"估计后相减"变成"按构造排除"，传统基线也第一次全部算齐并真正起到约束作用，这是把进度从 30% 提到 45% 的原因；机制验证和新知识发现仍未开始。 | 当前适应度接口未显示三个纯蛋白质模型超越语料检索，但这不等于模型只是在记忆。为越过这一界设计的二阶方案想用实测上位效应做减法，其语料耦合通道在 22 个蛋白质上全部无法预测实测上位效应，减法在当前接口上建不起来；仪器在文本对照上验证有效，所以那是一个测得的否定。真正的进展来自换掉评价对象：在检索被按构造排除、而不是估计相减的设计序列上，ProtGPT2 的稳定性排序优于语料自身的 3-mer、4-mer 片段统计，也优于语料组成——它相对语料统计的优势在排除检索之后依然成立；挡住它的不是检索，而是免费的物理化学基线，单参数的疏水性变化和在设计序列上远高于模型，二者之差为负且区间不含零。这是"相对基线排序"的否定，不是"模型是空的"：设计侧相关本身显著大于零，130 条设计里 97 条为正。识别力按臂计算、不能互借——只有 ProtGPT2 的训练语料就是被检索的数据库，ProGen2 见过未被检索的 BFD30，其数值只是偏向模型的界；其中最小一级连自身对照都没通过，按预注册规则它的设计侧不予读取，只记为仪器界限。一次事后的长度匹配对照表明这个反转属于"设计序列"而不是"短序列"。尚无经过验证的新生物机制或新生物学知识。 |

总体上，Objective 1 和 Objective 2 都已过半，而近期产出主要是撤回、收窄和把限制定位到具体环节，而不是新的正面结论。联合模型已经进入模型替换和 Model Diffing：替换的首次测量已记录但尚未解释，diffing 的两种模式都有了基线；跨模态概念对齐和 Circuit 翻译仍未开始。Objective 3 的前两个候选方法都已实现，并按各自的预注册关卡关闭——概念对齐 Lens 的前提被证伪，二阶检索界在第一道对照处停止——它们留下的是一条方法限制和一条关于语料耦合基线的界限，而不是生物学结论。第三次尝试不改方法而改评价对象，它给出的是一个测量结果而不是又一次停止：语料不相交的参照集已经建立并验证，检索第一次被按构造排除，模型相对语料统计的优势在排除检索之后仍然成立，真正挡住它的是一条免费的物理化学基线。这依然是一个否定结果——它没有证明模型学到了生物学，也没有证明模型是空的。这些否定结果是测出来的，不是没有做。
